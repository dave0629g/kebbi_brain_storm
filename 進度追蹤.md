# 進度追蹤（自主開發 loop）

> 我自己盤點「能獨立完成」的功能逐一做完；需要你提供的資訊集中在最下方「❓ 需要的資訊」，你填好我就接著做。
> 最後更新：2026-06-21(自測 **614/614 綠**。**【2026-06-21 新段落16】看著物件對話 RoboVisionTalk(#14)**:把 Robotics-ER 認物 + Gemini Live 語音兩條已驗管線合一。純核心 `src/App/VisionTalkContext.cs`(`SceneSummary` 去重保序限數、`SceneChanged` 集合比對節流、`VisionLine`/`BuildVisionTurnJson` 注入文字);Unity 組合:`GeminiLiveConversationBehaviour.InjectContext`、`RoboticsVisionBehaviour.OnSceneLabels` 暴露標籤、新 `RoboVisionTalkBehaviour` 同掛兩 behaviour + 橋接(場景變了才注入 Live)、`Mode.RoboVisionTalk` + 選單按鈕 + preflight。`T_VisionTalk` +11。⚠ Robotics-ER 免費額度緊(12s/拍)、Live 只能注入文字(視覺=旁白非真圖解),真機驗資源協調/額度。**【2026-06-21 新段落15】Live ephemeral token client 接口**:`GeminiLiveProtocol` 加 `WsUrlEphemeral(access_token)`(v1alpha,取代明文 `?key=` 降外洩)+ `IsPlaintextKeyUrl` 偵測;behaviour 加 `ephemeralToken` 欄位(設了就用 token 連線、否則退回明文)。barge-in 全雙工本就有 `gateMicWhileSpeaking`(戴耳機=false)。`T_LiveToken` +6。⚠ token 須後端短期簽發(開帳單)= 需你決策,client 接口先備。**【2026-06-21 新段落14】開機自檢面板 PreflightCore**:新增純函式 `src/Hardware/PreflightCore.cs`(`RequirementsFor(功能)`→所需資源 PreflightItem{語音/LLM/Gemini金鑰、麥克風、相機、網路};`Evaluate(需求,isOk)`→結果清單、`AllOk`/`Failing`/中文 `Label`)。`KebbiMenuBehaviour.Launch` 啟動功能前跑自檢、log 缺什麼(金鑰走 SecretsCheck、權限走 Permission、網路走 internetReachability)、選單顯示警示(非阻塞)。`T_Preflight` +11(需求對映/聚合/串接 SecretsCheck)。**【2026-06-21 新段落13】金鑰佔位偵測 SecretsCheck**:新增純函式 `src/Hardware/SecretsCheck.cs`(`IsPlaceholder`:空/太短<12/全同字元/範例字樣 your·changeme·placeholder·<>·你的·範例… → 佔位;`Check`/`ReportConfig` 盤點 SpeechKey/LlmKey/GeminiKey,**不回傳/不印任何金鑰值**)。`KebbiMenuBehaviour` 啟動盤點金鑰、缺/佔位時在選單顯示警示。`T_SecretsCheck` +16(空/短/範例→佔位、4 家真金鑰樣態→可用不誤判)。為 #7 preflight 的金鑰檢查項基礎。**【2026-06-21 新段落12】Gemini Live 斷線重連 + 生命週期**:`GeminiLiveProtocol` setup 一律帶 `sessionResumption`(空=請 server 發 handle)、新增 `BuildResumeSetupJson(handle)` 接續同 session、解析 `sessionResumptionUpdate.newHandle`/`goAway.timeLeft`、純 `LiveResumeState`(Observe 存 handle、ShouldReconnect、NextSetupJson 有 handle 帶之)。behaviour 接 `OnApplicationPause`(背景化停送麥克風,原 OnApplicationPause 零命中)+ `LiveResumeState.Observe`。`T_LiveResume` +10。完整無縫重連迴圈(音訊狀態重置)風險高、須真機驗 → 列後續。**【2026-06-21 新段落11】說話接力故事圈 StoryCircleGame**:把實測 7 分鐘的「語意端點」從沒真人受益者的測試轉成真小孩參與、凱比主持的多人接龍。新增 `src/App/StoryCircleGame.cs`:純 `StoryCircleCore`(點名輪替狀態機:小朋友 round-robin + 凱比每 N 位插一句、跨凱比延續不重頭、Story 累積)+ `StoryCircleGame`(點名轉頭 FaceFully + 小朋友 listen / 凱比 LLM 生句)。「點名制」一次一人規避多人 DOA 歧義。`T_StoryCircle` +9(輪替序列/凱比插話位置/Add/點名轉頭/game 端到端)。Mode 入口列下一步。**【2026-06-21 新段落10】觸控互動輸入層 ITouchSensor**:盤點指觸控是最被忽略的輸入(`src/` grep touch 零命中)。新增 `src/Hardware/ITouchSensor.cs`(`TouchZone` 頭/肚/左右手 + `OnTouch`事件/`Poll`輪詢)、`src/Sim/SimTouchSensor.cs`(Emit 注入)、純函式 `TouchIntents.Meaning`(摸頭=正向回饋/握手=交棒/摸肚=逗趣)、`TouchTurnToken`(握手=實體交棒 floor token,取代螢幕按鈕/網路 token)。`T_Touch` +11(意圖映射/事件vs輪詢/交棒狀態機)。Real 接 onTouchEvent 須真機驗(新必測項,先跑 touching.sh)。**【2026-06-21 新段落9】雙機口譯橋 TranslateRelayGame**:解決核心場景痛點(中文老師↔印尼學生聽不懂彼此)。新增 `src/App/TranslateRelayGame.cs`(純 IVoice/ILlm/IRobotLink):每台聽本地一句(localLang)→LLM 翻成對方語言(peerLocalLang)→經 link 送對方機→對方用本地語音說出;線格式 `TX|<譯文>`。純函式 `TranslateSystem`/`LangName`。`T_TranslateRelay` +11(SimRobotBus 雙機端到端一來一回 + 翻譯方向 + 空窗不空轉)。Mode 入口留下一步。**【2026-06-21 新段落8】真 UDP 環回回歸測試(T_UdpLoopback)**:既有 `T_RobotLinkProtocol` 只測記憶體編解碼,真 UDP 從未測過(必測④只驗 ping)。`T_UdpLoopback` 用真 `UdpClient`(loopback 隨機埠)實際送收 `RobotLinkProtocol.Frame` 封包,斷言一般訊息/空 text/中文 emoji/BC|VC| 樣式 round-trip + 垃圾封包安全回 false + 收後 ShouldDeliver。`--test` 即 CI → 鎖住多機 UDP 傳輸路徑進回歸網。只動 Tests.cs(console-only),不需 Unity 編譯。**【2026-06-21 新段落7】安全關鍵字老師可維護 + 熱重載**:`SafetyGateCore.Reload()` 原為空(核心刻意不可變)→ 新增 `src/App/Counselor/ReloadableSafetyGate.cs`(持可換 core,`TryReload` 成功才換、**失敗/空規則保留舊規則絕不無守門**);`UnityCounselor` 改用它,規則優先讀外部 `persistentDataPath/counselor_rules.json`(老師可維護)、無檔則用 build 烘進的內建範例,OnGUI 加「重載安全規則」按鈕。`T_SafetyReload` +10(換源生效/壞源例外保留/空規則保留)。內容仍須輔導專業定稿。**【2026-06-21 新段落6】STT 動態錄音窗(VAD 取代寫死 4 秒)**:新增純狀態機 `src/Hardware/VadEndpointer.cs`(`VadMath.Rms` + `VadEndpointer.Feed(rms,frameMs)`:偵測語音起點後連續靜音超門檻提早收窗、設 12s 硬上限保底、最短窗防誤收、短噪音<minSpeech 不誤判);`UnityVoice.ListenAsync` 從固定 4 秒盲錄改成邊錄邊看 RMS 包絡的動態窗(每 100ms 框餵 VAD,講完即收 + 裁切只送已錄段給 STT,`WavUtil.FromAudioClip` 加 sampleCount 多載)。`T_Vad` +10 合成 PCM 斷言。搶麥(Nuwa.stopListen)涉真 SDK 方法名未定 → 維持註解列真機必測①後續。**【2026-06-21 新段落5】會談摘要工具化(逐句落檔+交接卡輸出)**:新增純函式 `src/App/Counselor/HandoffCardFormatter.cs`(HandoffCard→老師一頁可讀文字,危急度第一眼、含主訴/層級/向度/重點/學生意願);`SimNotifyHuman` 補 `OnYellowQueued`/`OnSummary` 事件;`UnityCounselor` 用 `SimConversationLog(onAppendLine:落 persistentDataPath/counselor_logs/<sid>.jsonl)` 逐句 append-only 落檔、三類交接卡(🔴/🟡/📋)寫 `<sid>_handoff.txt`(老師可讀文字+JSON)。`T_CounselorHandoff` +11 斷言。⚠ 隱私「存哪/誰能讀/保存多久」須校方+使用者依規格定案,先落 app 私有沙箱不外傳。**【2026-06-21 新段落4】雲端退避守門層 CloudRetry**:demo 最常見當機點=雲端偶發 429/逾時,現況「一次失敗就 throw/回空」→ 畫面空白。新增純決策 `src/Hardware/RetryPolicy.cs`(`RetryPolicy.Next` 狀態碼+第幾次→是否重試+退避ms:2xx/3xx 成功、401/403/4xx 快速失敗、429/5xx/0 退避重試尊重 Retry-After、指數退避夾上限;泛型 `RetryLoop.RunAsync` 可注入 delay)+ console 轉接 `src/Cloud/CloudRetry.cs`(#if !UNITY 包 `Http.Client.SendAsync`)。OpenAiLlm/ClaudeLlm/AzureSpeech(TTS+STT)四處改用→Azure/OpenAI/Claude 三家 provider 全受惠。`T_CloudRetry` +18 斷言(決策表 + 假後端 429→200/持續500用盡/401立即停)。全程主控台假後端可測、免金鑰。**【2026-06-21 新段落3】RoboGuide 視覺認物→轉頭指物+念名稱**:`RoboticsVisionBehaviour` 原本只疊框、零動作/語音;新增純函式 `src/Hardware/RoboGuideMath.cs`(座標 0–1000→轉頭角:中央→0、左→-FOV/2、右→+FOV/2,含夾限/前鏡頭鏡像/box中點;`T_RoboGuide` +12 全綠),behaviour 接 Body/Voice、OnGUI 每個偵測加透明熱區 → 點它就 `KebbiHead.FaceFully` 轉頭朝它 + `Voice.SpeakAsync(label,"zh-TW")` 念名稱。FOV 可注入待真機現場校(必測③);Unity menu APK 編譯 RESULT=Succeeded errors=0。雙語(印尼語)念名稱列第二期(需 per-tap 翻譯)。**【2026-06-21 新段落】輔導具身共情 EmpathyBody(backlog 探索排名#1)**:`CounselorSession` 的 `_body`(原傳 `null`)接成真身體,`Speak()` 依安全燈號/說話時機(Login/GreenChat/Probe/Yellow/Red)用**免授權 setMotor** 做點頭(NeckY)/前傾/歪頭,學生開口讀 DOA 用 `FaceFully` 轉頭做眼神接觸。新增 `src/App/Counselor/EmpathyBodyCore.cs`(純 `EmpathyGestures` 映射 + `MotorEmpathyBody` 執行層,fire-and-forget+全程 try/catch+null-safe)、`UnityCounselor.cs:86` 改傳 `ctx.Body`(真凱比=馬達動作;一般 Android=SimKebbiBody no-op)、`T_EmpathyBody` +12 斷言。角度保守(|deg|≤25,NeckZ 實機 ±40),真機待對齊必測⑥微調幅度/時序。**全程 Sim 可測、不需真機/金鑰**。**【2026-06-21 新段落2】G3 鏡像體操教練接上選單**:`MirrorCoachGame`(已完整實作+測過)原本沒有 `Mode` 入口、選單到不了→實機完全跑不起來;加 `Mode.G3_MirrorCoach` + `RunMirrorCoachAsync`(示範版:`SimPoseSensor` 每拍預設過、不阻塞 STT,逐組示範暖身→太極→CPR)+ 選單按鈕「鏡像體操教練(G3)」。真姿態評分(MediaPipe)列第二期。**Unity menu APK 編譯驗證 RESULT=Succeeded errors=0**(非實測)。 ——以下為 2026-06-20 摘要—— **【本輪校正】STT 對話從「網路 floor token 交棒」改成「Kebbi↔真人 語意端點偵測」**:使用者點出 floor token 是作弊(真人不會送網路信號給 Kebbi),已重寫 `ConversationSttGame`——Kebbi 自己用「LLM 語意完整度 COMPLETE/INCOMPLETE + 靜音」判對方講完沒、INCOMPLETE 忍住不插話、IU 串接被切半的句子、COMPLETE 後留短 grace;第二支手機 `Persona.Human=true` 忠實扮真人(反 assistant、用 `…` 標停頓→分段 TTS 插真實停頓)。依研究 workflow w5rny9wai 落地、README 收 22 篇新論文。**✅ 兩台真機實測成功(2026-06-20 08:38)**:Black Shark=Kebbi(Andi)、ASUS Z011D=扮真人(Rina,印尼語),**內容走空氣(麥克風+Azure STT)、端點靠 Kebbi 自己語意判斷、全程零網路 token**。**連續約 7 分 8 秒(08:38–08:45)不卡死、~8 來回、主題連貫**(問路→指路→確認→道謝→延伸);Rina 全程維持人設(學生、會迷路、想借作業要用的書)且展現遲疑/自我修正/`…`停頓的真人腔。完整逐字稿見 `對話實測_2026-06-20.md`。**雷**:(1) MIUI/Black Shark 開機後彈 RECORD_AUDIO 權限框會把 Unity app 暫停 → 須 `adb shell pm grant ... RECORD_AUDIO` + `appops ... allow` 才不卡;(2) cascaded 批次 STT 使每輪延遲偏高(~30-60s),屬已知取捨。 ——以下為 2026-06-19 舊摘要—— 自測 **365/365 綠**。**多機 UDP 加了 unicast 直連備援**並用兩台實體 Android 真機驗證（發現此 WiFi 的 AP 丟廣播 → unicast 備援解決；詳見 DONE 第一條）。**純 Sim 高價值功能已全數收割（功能完成）**：本 session 連收 G1 Level3 / G3 動作幀 HoldMs·Loops / G2 甲機轉頭 NeckZ / G4 八向方位+最近可達扇區。**Unity 編譯/出 APK 已 DONE**（使用者另一 session 證實：`~/Projects/KebbiBrainUnity` Unity 2022.3.62f3，Assets/Scripts 以 **symlink** 接 robot_app/src 的 App/Hardware/Real/Sim、Config/KebbiFactory 為實檔、Cloud/ 刻意不接；`csc.rsp -define:UNITY` + `Editor/KebbiBuild.cs` 的 BuildApk/BuildMiddlewareApk → IL2CPP/ARM64 出 `Build/kebbi-arm64.apk` 與 `kebbi-middleware-arm64.apk` 各 ~9.5MB，含 src/Real AOT）。**剩餘高槓桿全在實機驗證（BLOCKED）**：把 `kebbi-middleware-arm64.apk` adb install 到手機跑 TTS/STT/LLM/UI、必測④雙機 UDP、馬達/DOA。詳見最下方「⏳ BLOCKED / ❓需要的資訊」）

---

## ▶ 如何接續（/compact 或新 session 後，讀這一段就能繼續）
**這份檔就是 backlog ＋ handoff。接手步驟：**
1. **環境**：.NET SDK 8 在 `~/.dotnet`（未進 PATH）。先 `export PATH="$HOME/.dotnet:$PATH"`。程式在 `robot_app/`。
2. **驗證現況**：`dotnet run --project robot_app/KebbiBrain.Sim.csproj -- --test`（目前應 **344/344 綠**；本機只有 .NET 10 → 需 `DOTNET_ROLL_FORWARD=Major`，見 [[dotnet-env-quirk]]）。
   各 demo：`--g1`（接力闖關）`--g2`（幾何接力）`--g3`（鏡像教練）`--g5`（法庭辯論）`--link`（多機）`--rv`（多機遠端語音）`--finale`（合體彩蛋接力大舞台）；G4 預設 demo；`--cloud-test`／`--target cloudsim`（需 Azure/OpenAI 金鑰）。
3. **挑下一項**：做「🔄 DOING」那項；沒有就做「📋 TODO」最上面一項。
4. **做法（沿用現有架構，勿另起爐灶）**：在 `robot_app` 用抽象介面 `IKebbiBody / IVoice / ILlm / IRobotLink / IPoseSensor` ＋ `Sim` 後端實作新邏輯 → 加自測（`Tests.cs`）→ build＋`--test` 全綠 → 把該項從 DOING/TODO 移到 ✅DONE、更新本檔。
5. **金鑰**：Azure（語音）＋OpenAI（LLM）已設為環境變數 `KEBBI_SPEECH_KEY`/`KEBBI_SPEECH_REGION`/`KEBBI_LLM_KEY`（provider 依金鑰前綴自動選）。⚠️先前外洩的金鑰記得 rotate。
6. **背景知識**：競賽/SDK 實測/骨架細節見 `~/.claude/projects/-Users-dave-chen-Projects-kebbi-brain-storm/memory/` 的 `kebbi-*.md`（每次 session 自動載入索引）。其他文件：`開發平台_可行性評估.md`、`技術實作路線_各隊.md`、`隊伍_G*.md`。

**接續 prompt（複製給我即可）**：
> 讀 `進度追蹤.md` 的「▶ 如何接續」，接手把 DOING／TODO 最上面還沒做完的做完，加自測跑綠，並更新該檔。

---

## 🧪 測試策略：三階（使用者 2026-06-18 定）
**核心：以 Android 為模擬器、Unity 為 middleware，在 Android 上跑「所有功能」測試（含多機互連），最後才上真凱比。**

| 階段 | 平台 | 機身(馬達/DOA) | 語音/LLM | 多機 | 用途 | 狀態 |
|---|---|---|---|---|---|---|
| **階1 主控台 Sim** | dotnet console | 模擬(文字) | Sim/真雲端(CloudSim) | 行程內 loopback | 邏輯規格、CI 自測 | ✅ 344/344 綠 |
| **階2 Android 中介** | 一般 Android(模擬器/手機,**非凱比**) + Unity middleware | **模擬**(SimKebbiBody) | **真** Azure 印尼語 + 真 LLM | **真 UDP**(UnityRobotLink) | 在 Android 上整合測試「除馬達/DOA 外的所有功能、含多機 UDP 互連」 | 🔄 程式已備(見下),待你在 Unity/Android 跑 |
| **階3 真凱比** | Kebbi(Android+NuwaSDK) | **真**(UnityKebbiBody) | 真 | 真 UDP | 上線實測(馬達/DOA/搶麥/相機) | ⏳ 需工作坊/實機 |

**階2 怎麼切**：Unity build 設 `Config.Target=Real` + `Config.UseRealRobotApi=false`（或 KebbiAppBehaviour 取消勾 `useRealRobotApi`）→ KebbiFactory 自動把機身換成 `SimKebbiBody`，語音/LLM/UDP 仍走真的。如此**不需要真凱比，就能在一般 Android 手機/模擬器把整條流程(含多機)跑起來測**。
- **多機互連測法**：2~3 台**真 Android 手機**接同一 WiFi，各跑 app 設不同 `RobotId`、`Mode=LinkPingTest` → 互收 ping 即 OK；再換各隊多機遊戲。
  ⚠️ **Android 模擬器(AVD)彼此網路隔離(NAT),UDP 廣播多半互通不到** → 多機測試請用真手機同網段;模擬器僅適合單機功能(語音/LLM/UI)測。
- **DOA 在階2**：無真麥陣列 → 用螢幕按鈕/腳本設定 `SimKebbiBody.CurrentDoa` 餵入(待補簡易測試 UI)。

---

## ✅ DONE
- **👁️‍🗨️ 看著物件對話 RoboVisionTalk(2026-06-21,#14 backlog 項)**：把專案兩條已驗管線(Robotics-ER 認物 + Gemini Live 即時語音)合一,讓凱比「看著真實世界陪你聊」(認物教學/看圖說故事/陪讀)。純核心 `src/App/VisionTalkContext.cs`(主控台可測):`SceneSummary`(標籤去重/保序/限數)、`SceneChanged`(去重集合比對 → 只在場景變了才注入,避免每 12s 同場景洗版/打斷語音)、`VisionLine`(「我現在用相機看到:X、Y。看著這些東西跟我聊」)、`BuildVisionTurnJson`(包成 Live clientContent 文字輪)。Unity 採「組合既有兩 behaviour + 橋接」最小侵入:`GeminiLiveConversationBehaviour.InjectContext(text)` 注入一輪文字、`RoboticsVisionBehaviour.OnSceneLabels` 回呼暴露標籤、新 `RoboVisionTalkBehaviour` 同掛兩者(Live 設視覺感知人格、RoboVision intervalSec=12 避 429)並把認物→`SceneChanged` 節流→`InjectContext`;`Mode.RoboVisionTalk` + 選單「看著物件對話(Live+視覺)」+ preflight(Gemini金鑰+相機+麥克風)。UI 天然分層(RoboVision 畫相機+綠框、Live 畫字幕)。`T_VisionTalk` +11(去重/限數/空場景/節流同集合不觸發/空↔非空觸發/順序變不觸發/VisionLine 含摘要/BuildVisionTurnJson clientContent+turnComplete+標籤)→ **614/614 綠**。⚠ Robotics-ER 免費額度緊(12s/拍,連續課堂需開帳單提額)、Live 只能注入文字(視覺=摘要旁白、非真圖像理解)、雙管線同時放大延遲/額度壓力 → 真機驗資源協調。
- **🎫 Gemini Live ephemeral token client 接口(2026-06-21,#16 backlog 項·client 端)**：對外展示前的安全前提——明文 `?key=` 進 URL 可能被 log/快取外洩。`GeminiLiveProtocol`:`WsUrlEphemeral(accessToken)` 改走 `?access_token=`(v1alpha 端點,以 Google Live API ephemeral token 文件為準上線前對齊)、`WsUrl` 重構共用 `WsBase(version)`(輸出不變)、`IsPlaintextKeyUrl(url)` 偵測是否明文金鑰直連。`GeminiLiveConversationBehaviour`:加 `ephemeralToken` 欄位 → 設了就用 token 連線、否則退回明文金鑰(接口先備,設 token 即生效)。barge-in 全雙工本就有 `gateMicWhileSpeaking`(戴耳機/外接 AEC 時設 false)。`T_LiveToken` +6(明文 key URL vs access_token URL、ephemeral 不含明文 key、v1alpha vs v1beta、IsPlaintextKeyUrl、null 安全)→ **602/602 綠**。⚠ token 由後端短期簽發(需 Google Cloud 開帳單 + 簽發端點)= 「需你決策」,本段落只落地 client 端接口;barge-in 受無硬體 AEC 物理限制仍須真機/耳機驗。
- **🩺 開機自檢面板 PreflightCore(2026-06-21,#7 backlog 項)**：demo 最怕「點下去沒反應」卻不知是金鑰/權限/WiFi。新增純函式 `src/Hardware/PreflightCore.cs`——`RequirementsFor(功能名)` 把每個 Mode 對映到所需資源(`PreflightItem`:SpeechKey/LlmKey/GeminiKey/Microphone/Camera/Network;如 LiveConversation=Gemini金鑰+麥克風、RoboticsVision=Gemini金鑰+相機、Counselor=LLM+語音+麥克風、ConverseStt=語音+LLM+麥克風+網路、G3=語音可降級);`Evaluate(需求, isOk)` 把需求對現況聚合成結果清單、`AllOk`/`Failing`(未過項)/中文 `Label`。`KebbiMenuBehaviour.Launch` 啟動功能前跑自檢:金鑰走 `SecretsCheck.IsUsable(Config.*)`、麥克風/相機走 `UnityEngine.Android.Permission`、網路走 `Application.internetReachability`,log 每項 OK/缺、選單顯示「X 可能無法正常運作,缺:…」(非阻塞,降級功能仍可跑、真正缺項由功能自身 UI 再提示)。`T_Preflight` +11(LiveConversation/RoboticsVision/Counselor 需求對映、未知功能無需求、全 ok→AllOk、缺麥克風→fail+Failing 列出+Hint=未授權、Label 中文、串接 SecretsCheck 真/佔位金鑰)→ **596/596 綠**。與 #17 SecretsCheck 串成完整「一進選單就知道缺什麼」。Unity menu APK 編譯驗證中。
- **🔐 金鑰佔位偵測 SecretsCheck(2026-06-21,#17 backlog 項)**：盤點列「輪替外洩金鑰 / 金鑰打包進 APK」待辦;佔位偵測低成本馬上降風險。新增純函式 `src/Hardware/SecretsCheck.cs`——`IsPlaceholder(value)`:空白/長度<12/全同字元/`...`/`null`·`none`·`test`/範例字樣(your、changeme、placeholder、example、todo、`<`、`>`、你的、範例、請填、尚未設定…)任一命中即佔位;`IsUsable`、`Check(name,value)→KeyStatus`(不含金鑰值,只回 OK/未設定/疑似佔位)、`ReportConfig()` 盤點 Config 的 SpeechKey/LlmKey/GeminiKey。`KebbiMenuBehaviour` 啟動後盤點並 log(不印值),缺/佔位時選單顯示「注意:有 N 個金鑰未設定/疑似佔位」→ 非工程的人(老師/評審)能自助排除「點下去沒反應」。`T_SecretsCheck` +16(空/空白/null/YOUR_API_KEY/`<your-key-here>`/changeme/你的金鑰/太短/全同字元/`...`→佔位;OpenAI·Anthropic·Gemini·Azure 真金鑰樣態→可用不誤判;Check 回報正確)→ **585/585 綠**。⚠ 只判「是否疑似佔位」,絕不回傳/印任何金鑰值。為 #7 preflight 面板的金鑰檢查項基礎;ephemeral token 等仍列後續。
- **🔄 Gemini Live 斷線重連協定 + Android 生命週期(2026-06-21,#9 backlog 項)**：實機雷——Android 切出 app 後麥克風/WS 行為未定義(`OnApplicationPause` 全專案零命中)、Live 又有 session 時長上限。協定層(`src/App/GeminiLiveProtocol.cs`,純 C# 可測):setup 一律帶 `sessionResumption`(空物件=請 server 開始發 resumption handle)、新增 `BuildResumeSetupJson(model,sys,handle)`(帶 handle 接續同一 session 不從頭)、`TryParseServer` 解析 `sessionResumptionUpdate.newHandle`(存起來)與 `goAway.timeLeft`、`LiveServerMsg` 加 `ResumptionHandle`/`GoAwayTimeLeft`、純狀態機 `LiveResumeState`(`Observe` 存最新 handle/timeLeft、`ShouldReconnect`=goAway、`NextSetupJson` 有 handle 帶之否則全新)。behaviour(`GeminiLiveConversationBehaviour`)接 `OnApplicationPause(pause)` → `_paused` 旗標、`Update` 背景化即停讀麥/停送音訊;`HandleServer` 接 `_resume.Observe(m)`、goAway 記警示。`T_LiveResume` +10(setup 帶/不帶 handle、解析 newHandle/timeLeft、狀態機存取與重連 setup 選擇)→ **569/569 綠**。⚠ 完整「無縫重連迴圈」(掉線→帶 handle 重建 WS + ring buffer/重採樣相位正確重置,workflow 點名最易出 bug)風險高且須真機驗 → 列真機後續;本段落落地可重連的協定/狀態/生命週期基礎。Unity menu APK 編譯驗證中。
- **📖 說話接力故事圈 StoryCircleGame(2026-06-21,#15 backlog 項)**：把剛校正、已實測 7 分鐘的「Kebbi↔真人語意端點」從「沒有真人受益者的測試場景」轉成真小孩參與、凱比主持的教育互動(多人接力是平板做不到的具身賣點)。新增 `src/App/StoryCircleGame.cs`(純 C#:IVoice/ILlm/IKebbiBody)——純 `StoryCircleCore` 是「點名制」輪替狀態機:小朋友 round-robin、凱比每 `KebbiEvery` 位插一句推進故事、小朋友序跨凱比延續不重頭、`Add` 累積故事(空句不接);`StoryCircleGame.RunTurnsAsync` 點名下一位→(小朋友)`KebbiHead.FaceFully` 轉頭面向他 + `ListenAsync` 聽一句 /(凱比)`LLM` 生一句說出。「點名制」一次一人 → 規避多人同講的 DOA 歧義(必測⑤風險)。`T_StoryCircle` +9(輪替序列正確/凱比插話位置/round-robin 跨凱比延續/Add 空句不接/點名小華 FaceFully NeckZ≈30/主持 game 4 回合端到端含小朋友+凱比句)→ **559/559 綠**。`Mode.StoryCircle` 入口 + DOA 轉頭點名真機列下一步。
- **✋ 觸控互動輸入層 ITouchSensor(2026-06-21,#10 backlog 項)**：盤點明指觸控是最被忽略的輸入(`src/` grep touch 零命中)——給不擅言語/低齡/特殊需求學生「碰觸而非開口」的極低門檻互動,純語意對話完全缺的非語言輸入維度。新增 `src/Hardware/ITouchSensor.cs`:`TouchZone`(Head/Belly/HandLeft/HandRight)、`ITouchSensor`(`OnTouch` 事件訂閱 + `Poll` 輪詢雙模)、純函式 `TouchIntents.Meaning`(摸頭→正向回饋、握手→交棒、摸肚子→逗趣)、`TouchTurnToken`(握手=「換你講」實體交棒 floor token,取代螢幕按鈕/網路 token)。`src/Sim/SimTouchSensor.cs`:`Emit` 注入觸控,有訂閱→事件回呼、否則進佇列供 Poll。`T_Touch` +11(意圖映射 / 事件模式即時回呼且不進佇列 / 輪詢依序取出 / 交棒 token 持有才交、摸頭不交、Grab 重獲)→ **550/550 綠**。⚠ Real 端(真凱比 registerRobotEventListener/onTouchEvent)是否受 active-code 授權牆 silent fail 須真機驗(新必測項);投入前先用 ~/Projects/UnityKebbi 的 touching.sh 在真機確認事件可達。互動邏輯本身全程 Sim 已測。
- **🌐 雙機口譯橋 TranslateRelayGame(2026-06-21,#11 backlog 項·核心)**：印尼語是核心場景但 Kebbi 原本只會自對話/出題,沒解決「中文老師↔印尼學生聽不懂彼此」的真實課堂痛點。新增 `src/App/TranslateRelayGame.cs`(純 C#:IVoice/ILlm/IRobotLink 抽象,主控台可測)——每台 Kebbi 各站老師/學生端當即時翻譯員:聽本地一句(`localLang`)→ LLM 翻成對方端語言(`peerLocalLang`)→ 經 link 送對方機 → 對方機用本地語音說出;反向同理。線格式 `TX|<譯文>`(送出已是對方語言、對方直接說)。純函式 `TranslateSystem(from,to)`(即時口譯員 prompt)/`LangName`(zh→繁中、id→印尼語、en→英語);`RelayOnceAsync`(聽→譯→送)、`SpeakIncomingAsync`(收→說)、`AttachSpeaker`、`RunAsync(maxTurns,maxEmpty)`(空窗不空轉)。`T_TranslateRelay` +11:`SimRobotBus` 雙機端到端——老師說中文→學生端機說出印尼語譯文、學生說印尼語→老師端機說出中文譯文、翻譯方向 system 目標正確、無人說話不送、RunAsync 連續空窗即停 → **539/539 綠**。複用已驗 Azure STT/TTS+LLM 落地;Gemini Live Translate 就緒可替換。`Mode.TranslateBridge` 入口 + 真機雙機實測列下一步。
- **🔌 真 UDP 環回回歸測試 T_UdpLoopback(2026-06-21,#12 backlog 項之一)**：盤點點出「真 UDP 沒測、失敗路徑沒測」。既有 `T_RobotLinkProtocol` 只測 `Frame`/`TryParse` 記憶體編解碼;`T_UdpLoopback` 補上**真 socket 收送**——兩個 `UdpClient` 綁 loopback 隨機埠(`ReceiveTimeout=2000`),`tx.Send` 框後 `rx.Receive` 解析,斷言:一般訊息 / 空 text / 中文+emoji+`BC|`/`VC|` 樣式皆 round-trip;垃圾封包(無 SOH 分隔)→ `TryParse` 安全回 false 不崩;收後 `ShouldDeliver` 對非自己/廣播為真。`--test` 本身即 GitHub Actions CI 跑的回歸網 → 多機 UDP 傳輸路徑入 CI(byte 邊界/UTF8 過線真的驗到)。+5 斷言 → **528/528 綠**。只動 Tests.cs(console-only,不需 Unity 編譯)。真機跨機 UDP 遺失/亂序仍須必測④定論。
- **🔑 安全關鍵字老師可維護 + 熱重載(2026-06-21,#8 backlog 項)**：規格要求「老師可維護關鍵字設定檔 + 可重載」,但 `SafetyGateCore.Reload()` 是空方法(核心刻意不可變、建構即定)、規則 build 時烘進場景字串。新增 `src/App/Counselor/ReloadableSafetyGate.cs`——`ISafetyGate` 實作,持「可換的」`SafetyGateCore`,`CounselorSession` 拿穩定參照、底下規則可即時換掉;`TryReload(out status)` 成功才換規則,**失敗(例外)或空規則一律保留舊規則**(安全鐵律:寧可用舊的,絕不讓安全閘變「無守門」)。`UnityCounselor`:規則來源改成優先讀「老師可維護」的外部檔 `Application.persistentDataPath/counselor_rules.json`,無檔則用 build 烘進的內建範例;OnGUI 加「重載安全規則」按鈕 → 老師更新外部 JSON 後即時生效。`T_SafetyReload` +10(換源後新關鍵字生效/舊失效、壞源例外保留舊規則、空規則保留、狀態標明)→ **523/523 綠**。⚠ 關鍵字「內容」仍須由校方輔導專業人員定稿(本檔只落地機制);規格「邊界一律往上靠(寧可誤報)」維持不變。Unity menu APK 編譯驗證中。
- **🎙️ STT 動態錄音窗:VAD 取代寫死 4 秒(2026-06-21,#6 backlog 項)**：固定 4 秒盲錄窗是 STT 對話時機不穩的根因(長句被切、講完硬等滿 4 秒才換手)。新增純狀態機 `src/Hardware/VadEndpointer.cs`——`VadMath.Rms(float[])` 算音框能量、`VadEndpointer.Feed(rms, frameMs)→bool` 端點偵測:偵測語音起點(RMS≥SilenceRms)後,語音累積≥MinSpeechMs 且連續尾靜音≥TrailingSilenceMs 就提早收窗;MaxWindowMs(12s)硬上限保底、MinWindowMs 防一啟動誤收、短噪音(<MinSpeech)不誤判為語音。`UnityVoice.ListenAsync` 改寫:`Microphone.Start` 開 12s 緩衝,每 100ms 框讀 `GetData` 算 RMS 餵 VAD,講完即 `Microphone.End` + `WavUtil.FromAudioClip(clip, total)` 新多載裁切「只送已錄段」給 Azure STT(省尾巴靜音/延遲)。`T_Vad` +10 合成 PCM 斷言(全靜音到硬上限/語音後尾靜音提早收/短噪音不誤收/連續講話到上限/最短窗)→ **513/513 綠**。搶麥(系統 wakeup 持麥 → Nuwa.stopListen)涉真 SDK 方法名未定、無法 device 測 → 維持註解列真機必測①後續。Unity menu APK 編譯驗證中。
- **📋 會談摘要工具化:逐句落檔 + 老師端交接卡輸出(2026-06-21,#5 backlog 項)**：把記錄/通知從「純記憶體+印字串」升級成輔導老師可帶走的工具。新增純函式 `src/App/Counselor/HandoffCardFormatter.cs`——`ToTeacherText(HandoffCard)` 排成「一頁就懂」:危急度第一眼(高/一般)、學生、時間、模式、主要關注、輔導向度(學習/生活/生涯/情緒/人際/家庭/安全 中文)、建議層級(發展/介入/安全)、情緒、學生意願、重點逐條、記錄連結。`SimNotifyHuman` 補 `OnYellowQueued`/`OnSummary` 事件(原只有 `OnHumanCalled`)。`UnityCounselor`:`SimConversationLog(onAppendLine: 寫 Application.persistentDataPath/counselor_logs/<sid>.jsonl)` 逐句 append-only 落檔(原 `UnityCounselor.cs` 沒傳 persist→寫完即失);🔴即時呼叫/🟡待辦/📋結束摘要三類交接卡寫 `<sid>_handoff.txt`(老師可讀文字 + 機器讀 JSON)。`T_CounselorHandoff` +11(摘要欄位齊全/None vs High/空 KeyPoints/落檔每輪觸發 append-only/三事件各一次)→ **503/503 綠**。⚠ 隱私決策:落檔「存哪/誰能讀/保存多久」(規格第十/十五節)須校方+使用者定案;目前先落 app 私有 `persistentDataPath` 沙箱(僅本機 app 可讀、不外傳),機制先備好。Unity menu APK 編譯驗證中。
- **🛡️ 雲端退避守門層 CloudRetry(2026-06-21,#4 backlog 項)**：把散落的「一次失敗就 throw/回空字串」收斂成共用守門。新增純決策 `src/Hardware/RetryPolicy.cs`——`RetryPolicy.Next(status, attempt, retryAfterSec)`:2xx/3xx=成功不重試、401/403/其他4xx=快速失敗、429/5xx/0(逾時或網路例外)=退避重試到上限;429 優先尊重 Retry-After、否則指數退避(base×2^(n-1))夾 MaxDelayMs;泛型 `RetryLoop.RunAsync<T>`(平台無關、delay 可注入)。console 端 `src/Cloud/CloudRetry.cs`(#if !UNITY)包 `Http.Client.SendAsync`(每次重建 HttpRequestMessage、catch I/O 例外回 status0、讀 Retry-After 標頭)。`OpenAiLlm`/`ClaudeLlm`/`AzureSpeech`(TTS+STT)四處 SendAsync 改用 → Azure/OpenAI/Claude 三家 provider 一次全受惠。`T_CloudRetry` +18 斷言(決策表 + 假後端 429→429→200、持續500送滿4次、401只送1次)→ **492/492 綠**,全程注入假後端/即時 delay、免金鑰。Unity 端 UnityWebRequest(RoboVision/UnityVoice/UnityLlm)的協程版退避列下一步(RetryPolicy 已可共用)。
- **👁️→🤖 RoboGuide 視覺認物→轉頭指物+念名稱(2026-06-21,#2 backlog 項)**：`RoboticsVisionBehaviour` 原本只把 Robotics-ER 偵測疊綠框/標籤、零動作零語音(grep 確認檔內 0 個 FaceFully/SpeakAsync)。本段落把它從「平板疊框」升級成只有機器人做得到的具身指認:新增純函式 `src/Hardware/RoboGuideMath.cs`——`AngleForX`(0–1000 正規化座標→水平角:中央500→0、左緣→-FOV/2、右緣→+FOV/2,含超界夾限)、`CenterX`(box 取中點/point 取 X)、`MirrorX`(前鏡頭鏡像)、`AngleForDetection`。behaviour:Start 經 `KebbiFactory.Create(Real)` 取 Body+Voice、選相機時記前/後鏡頭;OnGUI 每個偵測疊透明 `GUI.Button` 熱區 → 點它即 `PointAt`:算角→`KebbiHead.FaceFully` 轉頭朝它(輪式底盤+頭/H201頭部部分)+ `Voice.SpeakAsync(label,"zh-TW")` 念名稱,頂部提示列顯示「指著:X」。`T_RoboGuide` +12 斷言(角度映射/夾限/中點/鏡像/串 FaceFully)→ **474/474 綠**。FOV 預設 62° 可注入,光軸對 NeckZ 零點待真機現場校(必測③);雙語(印尼語)念名稱列第二期(需 per-tap 翻譯)。Unity menu APK 編譯 RESULT=Succeeded errors=0(未佈署/未實測)。
- **🤸 G3 鏡像體操教練接上選單(2026-06-21,#3 backlog 項)**：`MirrorCoachGame` 早已完整實作+大量測試(暖身/太極/CPR 逐幀示範、鏡像對映、HoldMs/Loops、喊「太快了」讀 DOA 轉頭降 BPM、「再一次」逐幀回退),但 `KebbiAppBehaviour.Mode` 沒有 G3、選單也到不了 → **實機完全啟動不了**(死碼)。本段落純接線讓它跑起來:`Mode.G3_MirrorCoach` 入口 + `RunMirrorCoachAsync`(沿用 `RunTebakArahAsync` 樣板:`KebbiFactory.Create(Real)` + H201 關底盤;`new SimPoseSensor` 示範版——空佇列預設回 `true`→每拍視為跟上、不觸發 STT、不阻塞)+ 選單按鈕「鏡像體操教練(G3)」。真相機姿態評分(MediaPipe)列第二期(依 RoboVision 額度/延遲再定)。**Unity menu APK 編譯 RESULT=Succeeded errors=0 warnings=0**(編譯驗證,未佈署/未實測)。
- **🤖💗 輔導具身共情層 EmpathyBody(2026-06-21,#1 backlog 項)**：把規格白紙黑字的「具身共情=平板做不到的賣點」從空殼補上。`CounselorSession` 原本把 `IKebbiBody _body` 傳 `null`、`Speak()` 只有一行空註解 → 現接成 `MotorEmpathyBody`，依安全燈號/說話時機映成小幅動作:Login=抬頭打開、🟢GreenChat=輕點頭「我在聽」、Probe=歪頭邀請、🟡=沉穩前傾停住、🔴=穩定前傾守住(最克制長停);學生開口讀 DOA 用 `KebbiHead.FaceFully` 轉頭做眼神接觸。純函式 `EmpathyGestures.MomentFor/GestureFor` 可斷言(`T_EmpathyBody` +12,**462/462 綠**)。鐵律:純加分、fire-and-forget、不阻塞對話、絕不丟例外、`body=null`(Sim/主控台)整層 no-op → 既有安全流程零影響。只用免授權 `setMotor`/`ReadDoaDegrees`,不碰授權牆擋的 LED/原生臉。真機待對齊必測⑥微調幅度/時序。
- **🗣️🏆 兩台手機印尼語對話(各有人格)連續 6 分 2 秒 PASS(2026-06-20)**：新 app `ConversationGame`(Mode.Converse)——兩台 Kebbi 各帶 persona,用真 LLM 生印尼語人格台詞、真 Azure TTS 說、靠「**說完才送 `CV|台詞` 的完成信號交棒**」一來一往(對方收到=知道我停了才開口,不猜時間);開場 **RDY 握手**(重送直到對方就緒)解決「starter 先講對方沒聽到」;逾時自我修正主動再開口不卡死。**UnityVoice 改等 clip 播畢才返回**(交棒精準)。實機:黑鯊(Andi,開朗,先說,.108)↔ ASUS(Budi,沉穩,.102),logcat 實測 **00:59:16→01:05:18 連續 6分2秒、37 輪、最大輪間隔 24.3s(無卡頓)、兩台全程存活**,人格分明、主題自然演化(問候→趣事→星星→朋友→英雄→韌性)。+6 Sim 自測(握手+2來回+交替歷史)→ **388/388**。螢幕 `ScreenLogHud`(OnGUI 鏡像 Debug.Log)讓對話直接顯示在手機畫面。build:`build-apk.sh converse` + `KEBBI_PERSONA_NAME/CHAR/PEER_NAME/CONV_STARTER` 注入。
- **🏆 兩台真機分散式 G1 PASS + ASUS 升 Android 6.0.1（2026-06-20）**：把第二台 ASUS ZE601KL 從卡死的 Android 5.0.2（API21 < APK minSdk22 裝不上）升到 **6.0.1 / API23**（升級法見 [[kebbi-test-phone]]：官方 OTA/解鎖全死 → 走「原廠 recovery `apply update from ADB` + adb sideload」，OTA 檔從 Internet Archive Wayback `id_` raw-replay 抓官方簽章原檔、並行 8 段下載繞限速、5.0.2 不能直跳 MM 故先 sideload 1.16.40.1524 墊腳再 MM .1698）。**達成真正的兩台真機完整多機測試**（從第一天就 BLOCKED 的項 b）：黑鯊(Blackshark DLT-A0, Android 11, .108)=`G1Director`(robotId=Kebbi-A)、ASUS(.102)=`Controlled`(robotId=Kebbi-B)。黑鯊跑 RelayQuestGame、把 B 機機身命令經 **UDP unicast** 送 ASUS → ASUS logcat `[RobotLink] 學到對端 192.168.1.108`(PeerRegistry 自動學習)＋ BodyCommandReceiver 真的執行 `🦾 RShoulderY/LShoulderY→100°`(終點舉雙手)、Move/Turn(SimBody 無輪故 log)。→ **unicast 備援＋分散式 G1 在兩台真機跑通**(之前只能用 Mac 當 peer 驗)。為此 `KebbiBuild` 加 `KEBBI_ROBOT_ID`/`KEBBI_PEER_ROBOT_ID` env 注入(每台不同 ID,否則協定把對方當自己回音丟)。**G5/G2 分散式也接著兩台真機驗過(2026-06-20)**:G5Director(黑鯊=控方)→ ASUS 收到辯方手勢 `🦾 RShoulderY 70°/100°`;G2Director(黑鯊=乙機)→ ASUS(甲機)收到走位+指認 `🦾 RShoulderY 60°/30°/80°`(角度與 POINT 送的 60/30/80 完全吻合)。→ **三隊分散式的「身體」路徑兩台真機全 PASS**。**剩**:G5/G2 遠端語音 `VC|SAY` 是否真從被控機喇叭播出(UnityVoice 不印 log,需人耳);真 Kebbi 馬達實動/DOA/搶麥。
- **🛰️ 多機 UDP「unicast 直連備援」+ 真機對真機實測（2026-06-19，兩台實體 Android）**：實測接上兩台手機（小米 Android 11／ASUS ZenFone 2 Android 5.0.2）發現**這個 WiFi 的 AP 會丟棄無線客戶端之間的廣播幀**——unicast 通、`255.255.255.255` 與子網廣播都收不到（用自編 `tools/udplisten.c` 在 ASUS 上驗：Mac/小米 unicast 收得到、broadcast 收不到）。**問題在網路層，不在程式**，但會讓只靠 UDP 廣播的多機發現失效。**修法**：`UnityRobotLink` 除照舊廣播外，另對「已知對端 IP」直接 unicast；新增純 C# 可測的 `PeerRegistry`（靜態設定 `AddStatic` ＋ 收包自動學習 `Learn`、去重、排除自己/萬用位址，**+13 自測 → 365/365**）；`KebbiAppBehaviour` 加 `peerIp` 欄位（逗號分隔多台）接進 5 處建 link；`KebbiBuild` 從 `KEBBI_PEER_IP` env 注入。**真機驗證**：同一個 app，廣播版 ASUS 收 0 包；build `KEBBI_PEER_IP=192.168.1.112` 的 unicast 版後，ASUS 實收小米送來的完整 RobotLink 封包（`Kebbi-A␁*␁ping #N`）→ **備援確實解掉真實 WiFi 丟廣播問題**。⚠️ ASUS（API 21 < APK minSdk 22）跑不了我們的 APK，故只能當網路接收端；**兩台都跑 app 的完整多機遊戲測試仍需 Android 5.1+ 的第二台**（見 BLOCKED）。
- 競賽研究（114 辦法/評分/時程）＋ 5 隊選題 ＋ 各隊獨立手冊 ＋ 點子庫 ＋ 合體彩蛋
- 開發平台可行性評估（CodeLab/RaaS/Unity）＋ SDK 實測整合（無移動/授權牆/DOA/關節…）
- 各隊技術實作路線（對抗式審查）＋ 印尼語雲端方案 ＋ 金鑰申請步驟
- `robot_app` 骨架：硬體抽象 ＋ 文字模擬器 ＋ `Sim/CloudSim/Real` 三層切換（LangVersion 9.0，可搬進 Unity）
- **G4《Tebak Arah》切片**：遊戲邏輯 ＋ 自測 ＋ **真雲端驗證通過**（Azure 印尼語 TTS/STT ＋ OpenAI gpt-4o-mini）
- **G3《鏡像體操教練》切片**：關節逐幀示範 ＋ DOA 轉頭調節奏 ＋ 姿態感測抽象（IPoseSensor/SimPoseSensor）＋ `--g3` demo
- **多機抽象 `IRobotLink` ＋ 雙機模擬**：行程內 loopback 匯流排（SimRobotBus/SimRobotLink）＋ Unity 實機桩 ＋ `--link` demo（雙機交棒握手 + 彩蛋廣播）
- **G2《幾何證明接力站》Sim 切片**：雙機說—走—指—接棒（GeometryRelayGame）＋ `--g2` demo（3 步等腰證明）
- **G5《法庭辯論劇場》Sim 切片**：雙機事件交棒接力辯論 ＋ 中央逼近(移動) ＋ DOA 轉向發言學生 ＋ 手臂指控/攤手（DebateGame）＋ `--g5` demo（伽利略審判）＋ 自測（共 42/42 通過）

- **G1《雙機接力闖關》Sim 切片**：指令序列直譯 → 雙機地板接力（RelayQuestGame）＋ `--g1` demo ＋ 自測（共 47/47 通過）。**至此五隊 G1–G5 Sim 切片全到齊**。
- **🎯 Unity 實機後端（RealBackends.cs，真 UnityWebRequest）**：UnityKebbiBody（NuwaRobotAPI）＋ UnityVoice（Azure 印尼語 TTS/STT，含 WAV↔AudioClip）＋ UnityLlm（OpenAI/Anthropic 自動選）＋ UnityRobotLink 桩 ＋ UnityWebRequest→Task 工具。形狀照 --cloud-test 已驗證的 REST。
- **`UNITY_接入指南.md`**：Unity 專案建立、SDK 匯入、`UNITY` 編譯符號、金鑰注入、MonoBehaviour 入口範例、build/部署(adb)、6 項上線必測。
- **🎯 Unity 後端三件套(實檔,皆 `#if UNITY`)**：
  ① `src/Real/UnityRobotLink.cs`——真 **UDP 廣播**收送(同埠廣播 + 收端過濾;網路緒收到 → 切主緒回呼);
  ② `src/Real/KebbiAppBehaviour.cs`——場景入口 MonoBehaviour,兩種單機模式 `G4_TebakArah` 與 `LinkPingTest`(後者就是必測④雙機收送的現成工具);
  ③ `src/Real/KebbiSecrets.cs`——金鑰注入用 ScriptableObject(`Assets→Create→Kebbi/Secrets`,`.asset` 記得加 .gitignore)。
- **🔌 多機協定抽出可測層 `src/Hardware/RobotLinkProtocol.cs`**：把實機 UDP 的「框架/解析/收件判斷」抽成純 C#,UnityRobotLink 直接用它 → **這段邏輯已在主控台自測(10 項,92/92)**;真正剩未驗的只有 UDP 傳輸本身(必測④)。
- **🧪 階2「Android 中介測試」接點(程式已備)**：`Config.UseRealRobotApi`(預設 true)；`KebbiFactory` Real 分支依此二擇一機身(true→UnityKebbiBody / false→SimKebbiBody)；`KebbiAppBehaviour.useRealRobotApi` 勾選同步。→ 設 false 即可在一般 Android 手機/模擬器跑真語音/LLM/UDP、模擬機身。console build 0/0、92/92 不受影響。
- **🕹️ 真機多台分散式控制層(純 C#,已自測 8 項)**：`src/Hardware/` 下 `BodyCommand`(機身動作↔文字命令)＋`RemoteBodyProxy`(IKebbiBody:每個寫動作經 link 送命令給另一台執行;讀回最後下達值)＋`BodyCommandReceiver`(被控機:收命令套到本機 body,非命令訊息轉交)。→ **一台中控用既有 G1/G2/G5 遊戲邏輯(bodyB 換成 RemoteBodyProxy)即可同時驅動本機＋遠端機,程式不改**。Sim 端用 SimRobotBus 全程自測過;實機只剩 UDP 傳輸(必測④)。
- **🎮 多機 Unity 入口(KebbiAppBehaviour 新增兩模式,`#if UNITY`)**：`G1Director`(本機當中控跑既有 G1 遊戲,遊戲內部 A↔B 握手走本機 loopback bus、只有機身命令出 UDP 到被控機)＋`Controlled`(本機當被控,只掛 BodyCommandReceiver)。→ 一台 G1Director＋一台 Controlled(robotId 不同、peerRobotId 互指)即可跑分散式多機。G2/G5 比照(換各自建構式)。console 0/0、92/92 不受影響。
- **✏️ 主控台打磨**：`--menu`／`--help` 命令清單 ＋ 邊界自測 27 項(方位扇區邊界 135/-135/180/-180、角度正規化 270/-270/360、印尼語詞往返、轉頭夾限邊界 90/-90/91/200、機身命令負值與速度參數)→ 當時共 **92/92**。
- **🗣️ 多機「遠端語音」層 `RemoteVoiceProxy`／`VoiceCommand`(純 C#,已自測 22 項 → 共 114/114)**：機身三件套的語音版 —— `VoiceCommand`(線格式 `VC|SAY|lang|text`,限數量切割保留台詞內的 '|'、lang 防呆、lang 預設值過 wire 正規化)＋`RemoteVoiceProxy`(IVoice:`SpeakAsync` 經 link 送 VC 命令讓「被控機用自己的喇叭」說;`ListenAsync` 不支援遠端聽→回空字串並 log 警告)＋`BodyCommandReceiver` 擴充(加 `IVoice localVoice`,分派鏈 BC→VC→alsoHandle)＋真機 `KebbiAppBehaviour.RunControlled` 接上 `ctx.Voice`。→ **G5 辯方/G2 乙機 Director 模式下,把 `defVoice` 換成 `RemoteVoiceProxy` 即可讓對方機「自己開口」,DebateGame 程式不改**。經設計階段 3 視角對抗式審查(抓到 IndexOutOfRange/lang 不過 wire/Split 多載可攜性等 8 項並修正)。⚠ `SpeakAsync` 合約降級:送出即返回不保證播畢(真機 UnityVoice 本就如此),真機嚴格交棒需補 ACK/DONE 握手(見 BLOCKED)。
- **🎮 示範小遊戲 `--rv`**：跑「未改一字」的 DebateGame,但辯方=遠端被控機 → 辯方台詞與手勢由被控機自己的喇叭/機身執行(主控台可見 `VC|SAY`/`BC|*` 命令越過 loopback「網路」抵達被控機)。實機把 SimRobotBus 換成 UnityRobotLink(UDP)即同款。
- **🎪 合體彩蛋《凱比聯合學園祭》編排器 `FinaleShowGame`(純 C#,已自測 15 項 → 共 129/129)**：對應 `合體彩蛋_G2G3G5_多機協作.md`。中控導演機依序 cue 各關卡站(G2 幾何/G3 律動/G5 對峙,`CUE|role`)→ 站台回 `ACK|role`(開始)/`DONE|role`(完成)→ 中控收 DONE 才往下;壓軸廣播 `FINALE` → 全體走位中央 + 同步勝利動作(中控也舉手)。**★降級備案(doc 標「最重要」):站離線(無 ACK)或卡住(ACK 無 DONE)→ 中控自動跳過,其餘照跑、壓軸照常 → 合體翻車不拖垮全場**。自測涵蓋:三站全到/一站離線/一站卡住/全站離線四情境。
- **🎮 示範小遊戲 `--finale`**：三場——①三站全到的完美接力(壓軸全體舉手到 100°);②G5 夥伴機臨時離線 → 中控自動跳過、壓軸照跑;③G2 站非同步延遲回應 → 中控 await 等到才算完成。主控台可見 `CUE/ACK/DONE/FINALE` 訊息流。
- **🎉🎉 用 Unity 出了一個「合規可裝機」的 Kebbi Android APK(整條 build 鏈打通)**：在 `~/Projects/KebbiBrainUnity` 寫 `Assets/Editor/KebbiBuild.cs`(設 arm64/IL2CPP/minSDK22/targetSDK33/package=com.kebbibrain.app,掛 `KebbiAppBehaviour` 防 strip 確保 src/Real 進 AOT)→ batchmode `-executeMethod KebbiBuild.BuildApk`。**RESULT=Succeeded、0 錯 0 警告**,產出 `Build/kebbi-arm64.apk`(9.6MB)。aapt 驗證:`native-code: arm64-v8a`(無 armv7)、`sdkVersion 22`、`targetSdkVersion 33`、`lib/arm64-v8a/libil2cpp.so`(10MB,IL2CPP 把全部 managed 含 src/Real 編成原生)、NuwaSDK aar 已合併(manifest 帶入 BLUETOOTH 權限)。→ **robot_app 從 Sim 邏輯(153/153)→ #if UNITY 後端 → IL2CPP arm64 原生 → 合規 APK 整條鏈驗通**;剩下只差實機安裝執行。**出了兩支 APK**:`kebbi-arm64.apk`(實機版 useRealRobotApi=true,只跑真 Kebbi)＋`kebbi-middleware-arm64.apk`(中介版 useRealRobotApi=false → 可跑一般 Android 手機/模擬器測語音/LLM/UI,階2 策略)。build 指令(`KebbiBuild.BuildApk` / `BuildMiddlewareApk`):需 sandbox 關、~9 分。⚠️ **第二次 batchmode 開啟同專案會卡死 → 每次 build 前 `rm -rf Library Temp Logs`**(見 memory)。
- **🎉 `src/Real` 整段 `#if UNITY` 後端在真 Unity 編譯成功(清掉「這台無 Unity 無法編譯」的最大 BLOCKED)**：建獨立 Unity 專案 `~/Projects/KebbiBrainUnity`(2022.3.62f3,**非** UnityKebbi 參考專案)→ 把 `robot_app/src` 的 Hardware/Sim/App/Real/Config/KebbiFactory 以 symlink 接進 `Assets/Scripts`(單一來源)+ `Assets/csc.rsp` 開 `-define:UNITY` + NuwaSDK aar 進 `Assets/Plugins/Android` → batchmode 編譯(buildTarget Android)。結果 **0 error / 0 warning**,`Library/ScriptAssemblies/Assembly-CSharp.dll` 含全部 9 型別(UnityKebbiBody/UnityVoice/UnityLlm/UnityRobotLink/KebbiAppBehaviour/KebbiSecrets + RemoteVoiceProxy/FinaleShowGame/LinkAwaiter)。→ **先前只能「對 aar 反查」的 `#if UNITY` 程式現在真的在 Unity 編過了**(含 `ctlMotor` 修正);robot_app/src 內的 `.meta` 已 gitignore。重編指令見下方 BLOCKED 區。剩下真機驗證(出 APK 裝機跑)仍需實機。
- **🔩 src/Real 馬達 API 真機 bug 修正(對真 NuwaSDK 2.1.0.08 aar 反查,自測 +10 → 153/153)**：用參考專案 `~/Projects/UnityKebbi` 內附的真 aar 以 javap 反查 `NuwaRobotAPI` 實際 public 方法,發現 `UnityKebbiBody` 兩個方法名是錯的、實機會丟 NoSuchMethod:① 設角 `setMotorPositionInDegree` **不存在** → 真 API 是 `ctlMotor(motorId,度,速)`;② 讀角 `getMotorPresentPossitionInDegree` **拼字錯** → `getMotorPresentPositionInDegree`(單 s)。`getInst()/move/turn/getDirectionOfDOA` 反查確認**正確**。馬達 ID:`KebbiMotor` enum 值 == SDK `MOTOR_*` 常數(NECK_Y=1…LEFT_ELBOW_Y=10),已加純 C# `T_NuwaMotorIds`(10 項)在主控台釘住。→ 這是「只有對著真 SDK 才驗得出」的實機 bug,靠 UnityKebbi 當參考解掉。`ctlMotor`(度,速)語意仍待實機量測(必測⑥)。
- **🗣️ 遠端語音「說完才交棒」握手 `VC|DONE`(純 C# 已自測 6 項 → 共 143/143)**：`VoiceCommand` 加 `Done()`/`IsDone()`/`TryParseSay()`;`RemoteVoiceProxy` 加「等播畢」模式(傳入 `LinkAwaiter` → `SpeakAsync` 先註冊 DONE 等待者再送 SAY、await 被控機回 `VC|DONE` 帶逾時才返回);`BodyCommandReceiver` 加 `ackVoiceDone`(收 VC|SAY → 等本機 voice 播畢 → 回 `VC|DONE`)。→ **G5 DebateGame「辯方說完才換控方」在真機也正確**(把 defVoice 換成等播畢的 RemoteVoiceProxy、被控端設 ackVoiceDone)。預設仍 fire-and-forget(向後相容)。自測:延遲語音證明 await 真等到播畢、fire-and-forget 對照、逾時不卡死。`--rv` 加「後記」場景示範 VC|DONE 回流。解決 cycle 1 review 標的真機 TODO。
- **🔗 `LinkAwaiter`(IRobotLink 上的 await+逾時 request/response 助手,純 C# 已自測 6 項 → 共 137/137 時)**：提供 `WaitForAsync(predicate, timeoutMs)`,用 TaskCompletionSource + Task.Delay,「先註冊等待者再送觸發訊息」→ **Sim 同步遞送與真機 UDP 非同步遞送皆正確**。**用它把 FinaleShowGame 的跳過判定從「送 CUE 後同步讀旗標(只 Sim 成立)」改成「await ACK(逾時→離線)/await DONE(逾時→卡住),且只接受 from==該站」** → 解決兩輪 review 同列的 #1 真機問題(舊版真機會每站誤判離線)。自測新增「站台延遲後才回 ACK/DONE」的非同步路徑案例,證明 await 真的等到(非舊同步路徑)。經 2 視角對抗式 review(並發正確性 + 真機)。

## 🔄 DOING（本回合）
- （無）Unity 後端三件套本回合已寫完並串接;Unity 端真正驗證為 BLOCKED(這台無 Unity)。

## 📋 TODO（我可獨立做）
1. 〔階2 支援・#if UNITY 待實機驗〕KebbiAppBehaviour 加「螢幕測試 UI」(按鈕設模擬 DOA、選隊伍、顯示 log)。
   - ~~對方機要「自己說話」需 `RemoteVoiceProxy`~~ ✅ **已完成**(協定/分派/被控端接收/真機接點皆已做、22 項自測綠、`--rv` 示範)。
     **剩未做(#if UNITY,待實機)**:在 `KebbiAppBehaviour` 新增 `G5Director`/`G2Director` 兩個 Mode(比照既有 `G1Director`,把 `defVoice` 換成 `RemoteVoiceProxy`、`defBody` 換成 `RemoteBodyProxy`)→ 讓 G5/G2 也能實機多機跑。此為 Unity 入口接線,本機無 Unity 無法編譯驗證 → 歸 BLOCKED。
2. 各隊「初賽 3 頁教案格式」草稿（純文件；**待主攻確認**才動）。

### 🚀 平行研究+平行實作（使用者 2026-06-18「多個功能平行同時研究」）
6 agent 平行研究產出可實作規格 → 4 個「動的遊戲檔不重疊」功能用 4 worktree agent **平行實作**(各自 build+test 綠)→ 主迴圈整合(零衝突)。一次 191→273/273:
- **G5 七步審判驅動器**(`RunTrialAsync`:開庭→陳述→交棒→反駁→逼近→轉向學生→結辯)+ **學生席舉手插話分支**(姿態 gate→轉頭→學生麥→改票);`--g5t`;+22 測試。
- **G4 裁判賽**(A 描述 B 方位、Kebbi 用 DOA 真值核對計分)+ **視角轉換**(`Direction.RelativeDir`:我的右=Kebbi 的左)+ **多輪排名**(`RunTournamentAsync`/Ranking);`--g4t`;+12 測試。
- **G3 逐幀回退**(`CurrentFrame` 索引 + `RewindOneFrame`/`HandleAgainAsync`:喊「再一次」回退一幀重示範);`--g3r`。
- **G2 學生自編走位腳本驗證器**(`ValidateScript`:缺步/多步/層次錯置/順序錯→指出第幾步哪種錯,通過才接力);`--g2v`;+11 測試。
- **(序列批)G1 交棒「換你」語音+舉手手勢**(建構式加可選 voiceA/voiceB:A 舉手「換你！」→ B「收到，換我！」→ A 放手) + **G1 交接點同步條件**(LevelMap 加 'H' 交接點 + Level2 轉彎走廊;A 沒走到 H 就 HANDOFF=交棒失敗 B 不啟動);+9 測試;`--g1` 加交棒演出 + 關卡2。→ 平行研究的 6 功能全部完成(273→282/282)。

### 📦 純 Sim 遊戲深度 backlog（多代理 survey 2026-06-18；皆本機可驗、可配 demo，供 loop 逐項收割）
> 結論修正：先前寫「高價值項見底」**不再成立**——五隊遊戲性盤點出 ~32 個純 Sim 缺口。三條高槓桿主軸:① 寫死腳本→資料化關卡庫＋場次驅動器(RunSession/關卡 Map);② 補 Score/PrintSummary 結算(G1、G5 尚缺);③ 把剩餘同步讀旗標遷到 LinkAwaiter(G2)。
- **G1（最薄,優先）**：~~①關卡地圖+障礙/避障條件積木~~ ✅ **已做**(commit 見下,`LevelMap`+`RelayQuestGame` 加地圖/朝向/撞牆 Crashed/`IF_OBSTACLE`/`IF_CLEAR`/`Reset` 可重入,9 項自測,`--g1` 三回合 demo 撞牆→繞行→智慧避障;無地圖=舊行為向後相容)。 ~~②闖關計分/星等 PrintSummary~~ ✅ **已做**(commit 見下,Attempts/TotalCrashes/Stars 星等[以 BFS 最短路徑為基準]/PrintSummary;5 項自測;`--g1` 結算「第N次嘗試、累計撞牆、效率★★★」) ③~~失敗重試閉環 Reset~~(Reset 已做,可再加多份程式對比) ~~④難度分層多關卡~~ ✅ **已做**(Level1 一處障礙、Level2 轉彎走廊+交接點 H、Level3 多障礙 S 形強制路徑+交接點 H;`T_G1_Level3` 6 項自測:撞牆版撞 #(1,3)、正解 7 步==最短 3 星、可重入;`--g1` 加關卡3 demo) ~~⑤交棒點同步條件~~ ✅(沒走到交接點就交棒=失敗,Level2/3 皆驗) ~~⑥交棒「換你」語音+舉手手勢~~ ✅。**→ G1 純 Sim 缺口全清。**
- **G2**：~~①交棒握手改用 LinkAwaiter~~ ✅ **已做**(commit 見下,送 POINT→await DONE 帶逾時,甲機逾時降級 StepsSkipped、可重入;3 項自測;`--g2` 加離線降級場景)。 ~~②學習單(已知/因為/所以)學生作答驗證+糾錯~~ ✅ **已做**(commit 見下,Step.Layer 可選 + 乙機問層次/學生答/Score/答錯提示;MakeIsoscelesProofWorksheet;4 項自測;`--g2` 加學習單;無 Layer 向後相容) ~~③多回合 session+結算~~ ✅ **已做**(commit 見下,RunSessionAsync 跑題庫多題+累計步數/學習單得分+PrintSessionSummary;4 項自測;`--g2` 跑完整題庫場次) ~~④證明題庫(目前只 1 條等腰)~~ ✅ **已做**(commit 見下,MakeAngleSumProof 內角和/MakeExteriorAngleProof 外角定理 + MakeProofLibrary 3 題具名;5 項自測;`--g2` 題庫換題) ~~⑤學生自編走位腳本 validator~~ ✅ **已做**(audit 核對:ValidateScript 覆蓋缺步/多步/層次錯置/順序逆序/缺標層 + RunProofIfValidAsync 驗證過才接力;`--g2v` demo)。~~⑥甲機轉頭望向發言者(NeckZ)~~ ✅ **已做(本輪)**(Step.TurnHeadToward 可選角度[可為負=望左],POINT 協定帶轉頭欄位;甲機收 POINT 先 SetMotor(NeckZ)轉頭視線跟隨再手臂指認;MakeIsoscelesProofTurnHead -45/0/45;`T_G2_TurnHead` 10 項自測[負角有效/0°望前/null 不轉/舊式 4 參數相容/可重入];`--g2h` demo;null=不轉頭向後相容)。**→ G2 純 Sim 缺口全清。**
- **G3（最成熟）**：~~①「再一次」回退一幀~~ ✅ **已做**(audit 核對:RewindOneFrame/HandleAgainAsync 純狀態回退+夾邊界+可重入;`--g3r` demo) ~~②多動作 session(太極/CPR)+語音切換下一組~~ ✅ **已做**(MakeTaichi/MakeCpr/MakeDefaultRoutine + RunSessionAsync;5 項自測;`--g3`) ~~③動作幀資料化(每幀停留/BPM/循環)~~ ✅ **已做(本輪)**(JointFrame.HoldMs 單幀自訂停留覆寫 BPM、Move.Loops 整組循環、FrameHoldMs/FramesPlayed 可純驗;`T_G3_Frame` 12 項自測;`--g3f` demo CPR 下壓停 2 秒+暖身 3 組+降 BPM 只影響未自訂幀;Loops=1 且無 HoldMs=等價舊版向後相容) ~~④左右鏡像對映~~ ✅ **已做**(MirrorMotor/MirrorMove;5 項自測;`--g3`)。**→ G3 純 Sim 缺口全清。**
- **G4**：~~裁判賽三角關係、視角轉換(RelativeDir)、排名(Ranking)~~ ✅ **已做**(audit 核對:JudgeRoundAsync/RelativeDir/PerspectiveRoundAsync/RunTournamentAsync/PrintRanking,12 項自測;`--g4t` demo)。~~八向方位升級~~ ✅ **已做(本輪)**(Direction 4 向→8 向[含斜向 serong kanan/kiri、belakang kanan/kiri,每區 45°];ParseIndo 複合詞優先解析;SectorCenterDeg/NearestReachable 最近可達扇區;KebbiHead.TurnToward 多載回報「夾限後實際面向扇區」=馬達可達性決定判決;`T_G4_EightWay`+邊界共 +25 項自測;`--g4e` demo;舊 4 向/3 參數多載完全向後相容,FromAngle 僅 G4+DOA log 消費,無他隊回歸)。
- **G5**：~~結辯投票計分~~✅ ~~學生發言分支(StudentInterjection)~~ ✅ ~~7 步驅動器(RunTrialAsync)細化~~ ✅ **全做齊**(audit 核對:RunTrialAsync 7 步 + HandleInterjectionAsync 姿態 gate→轉頭→學生麥→改票 + ConcludeAsync 宣判;`--g5t` demo)。候選下一功能:InterjectionScorer 插話加權計票(M,較大、較投機,優先度低)。
> 完整 32 項見 workflow 輸出 `tasks/wsxx7iwt8.output`。

> **🧭 loop 恢復點(2026-06-19,共 352/352 綠)**:已連收 G1 Level3、G3 動作幀、G2 甲機轉頭、G4 八向方位、NeckZ ±40、FaceFully 複合面向、**FaceFully 接進 G4/G3/G5(經審判式 review→修 3 must-fix→過)**、**安全金鑰注入 build 流程(env→asset→APK,key 不落地)** 等段落。**慣例**:每 commit 後直接 push([[push-after-commit]]);auto/dev-loop 與 main 都推 origin。**已合併進 `main` 並 push。****G1/G2/G3/G4 純 Sim 重點缺口全清**;G5 核心早已做齊。NeckZ ±40 後:G4/G3/G5「面向側邊學生(>±40)」誠實回 reachable=false(部分面向),既有測試已改為實機真值。
>
> ~~**🆕 SDK 數值揭露的新候選 FaceFully**~~ ✅ **已做(2026-06-19,使用者指示「做 FaceFully」)**:`KebbiHead.FaceFully` — 底盤 `turn()` 轉粗方向 + NeckZ(±40)補細;輪式(CanMove)可完整面向任意角(含正後 180°=底盤140+頭40);H201 桌上型不能轉底盤 → >±40 誠實部分面向(FullyFaced=false)。`FaceResult`(BaseTurnDeg/HeadDeg/FacedAngle/FullyFaced)。`T_FaceFully` 7 項自測;`--face` demo(30/90/180/-120 + H201 對照)。實機 turn() 受授權牆(必測③)、turn 正負向待核對;被擋則退回頭部部分面向。**已接進 G4(Forward/Reverse/Judge/Perspective)、G3(HandleTooFast)、G5(TurnToStudent) 全部「轉向學生」呼叫**(使用者 2026-06-19 指示);輪式機(G5 會逼近移動=wheeled)現在 demo 真的「底盤轉 80°+頭 40°→完整面向」120° 的插話學生;G4/G3 預設用 H201 桌上型故維持頭部部分面向;352/352 不變(既有測試多為桌上型或 ±40 內,語意一致)。
>
> **🛑 純 Sim 開發判定「功能完成」(2026-06-19,此前)**:最後候選 G5 `InterjectionScorer` 經讀碼判定**不值得做且該砍**——理由:`DebateGame.TrialScript.Interjections` 是 `Dictionary<int, StudentInterjection>`(key=回合索引),**結構上每回合至多一筆插話**;該候選的賣點(同一學生多次插話的「票數防灌水」、「幾位學生舉手的加權」)在現行模型**根本不會發生**,且 `Interjections` 計數已等同「被插話回合數」(每回合≤1)。要做它得先把插話模型改成「多筆/回合」=沒人要求的 scope creep。→ 結論:**純 Sim 高價值功能已全數收割**(三鏡頭提案+評審 wjgy32z4o 亦同此判)。
> **🤖 實機自動測大進展(2026-06-19,使用者接了 Android 手機 [[kebbi-test-phone]] 授權自動測)**:用 env 注入金鑰的變體 APK + 這台 Mac 當 LAN peer,**全自動(免互動)驗過**:① 階2 中介版 TTS/STT/LLM 三管線跑通(金鑰有效)② 金鑰注入流程(env→asset→APK→清空,不外洩)③ 必測④ UDP **雙向**通(手機↔Mac,framing 正確、AP 不擋)④ G1 分散式**中控發送鏈**(Director→7 正確 BodyCommand)⑤ 被控機 **Controlled 收命令執行**(Mac→手機 SetMotor/Move/Turn,motorId 4=RShoulderY 證實 enum==SDK)。→ **雲端管線 + 多機分散式兩端,皆在真 Android 驗過**。權限免彈窗用 `adb install -g`。
> **🐛 G5 分散式測抓到並修真 bug(2026-06-19)**:加 `Mode.G5Director`(法庭辯論辯方機身+語音遠端,比照 G1Director)後實機跑,卡在控方第一句 TTS——因為 **Azure 聲線寫死 `id-ID-GadisNeural`(印尼語),但 G1/G2/G3/G5 台詞是中文(zh-TW)**,用印尼聲線合成中文被 Azure 拒 → 4 個中文遊戲 TTS 在實機全會卡。**已修**:`Config.VoiceForLang(lang,voice)` 依語言自動選聲線(zh→`zh-TW-HsiaoChenNeural`、id→印尼聲),接進 UnityVoice + AzureVoice;cloud-test 加中文 TTS 驗證(✅ 140KB 中文音檔合成、聲線 zh-TW-HsiaoChenNeural)。352/352 不變。→ **含修正重 build 後,完整分散式 G5 實機 PASS**:Mac 解碼到辯方遠端 **3 語音(`VC|SAY|zh-TW`:兩回合反駁台詞+「辯方勝訴」)+ 3 機身(`BC|SM` 攤手70°×2+舉手100°)**,完全對應伽利略辯論。→ RemoteVoiceProxy(遠端語音)+ 分散式 G5 在實機驗過。G1+G5+**G2** 三種團隊遊戲分散式入口**皆完成並實機驗過**:G2Director 跑通,Mac 解碼甲機遠端 **7 機身命令**(走位 Move/Stop + 手臂指認 SetMotor RShoulderY=60/30/80°,對應等腰證明 3 步),乙機中文理由也出得了聲。→ **多機分散式全套(G1雙機/G5法庭/G2幾何)在真 Android 驗完**。
> **單機自動測已到頂**。**真正剩(需硬體/人)**:兩台真機/真 Kebbi 一起跑完整遊戲(輪子實走/DOA/搶麥)、TTS 發音品質(人耳)、真人講印尼語 STT 準確度、`必測③ turn() 授權`。loop 仍「等使用者實機/資料」:確認各隊主攻→寫教案;給第 2 台/真 Kebbi→跑完整多機。
> ✅ **Unity 編譯/出 APK DONE**(KebbiBrainUnity 2022.3.62f3、symlink 接 src、IL2CPP 出 5 種變體 APK:real/middleware/linkping/g1director/controlled,`unity-build/build-apk.sh`)。

> 方向（使用者 2026-06-18）：**專心做 Unity**，Blockly/CodeLab 太多功能做不到。Sim 層 = 已驗證的邏輯規格(5 隊 47/47)；接下來把 `src/Real` Unity 後端做實、做完整，並用 `UNITY_接入指南.md` 讓你能在 Unity 開起來。**Unity 端的真正驗證要在你的 Unity/實機上做（我這台沒有 Unity）。**

## ✅ 已排除（曾 BLOCKED，現已驗證解除）
- **Unity 後端編譯 + 出 APK**　✅ **DONE（2026-06-19 使用者另一 session 證實）**：`~/Projects/KebbiBrainUnity`（Unity **2022.3.62f3**）。`Assets/Scripts/` 下 **App/Hardware/Real/Sim 四個皆 symlink** 指向 `robot_app/src/*`（改 src 即時雙向同步），`Config.cs`/`KebbiFactory.cs` 為實檔；**`Cloud/` 刻意不接**（Unity 走 `Real/RealBackends.cs` 的 `UnityWebRequest`，Cloud 的 AzureSpeech/OpenAiLlm/Http 維持 console 專用）。`Assets/csc.rsp = -define:UNITY`；`Assets/Editor/KebbiBuild.cs` 提供 batchmode 入口 `BuildApk`（真機版 `useRealRobotApi=true`）/`BuildMiddlewareApk`（中介版 `false`）。**`Build/` 已產出 `kebbi-arm64.apk` + `kebbi-middleware-arm64.apk`（各 ~9.5MB，IL2CPP/ARM64 AOT 過、含整段 `#if UNITY` 的 src/Real）** → 證實 src/Real 在真 Unity 編得過、能出合規 APK。重編：`rm -rf ~/Projects/KebbiBrainUnity/{Library,Temp,Logs}` 後 `"$UNITY" -batchmode -quit -projectPath ~/Projects/KebbiBrainUnity -buildTarget Android -executeMethod KebbiBuild.BuildMiddlewareApk -logFile /tmp/kbu.log`（見 [[kebbi-autodev-loop]] 的 batchmode 第二次卡死坑）。

## ⏳ BLOCKED（梳理 2026-06-20：軟體/網路/多機已全清，剩下只剩三類）
> 兩台 Android 真機（黑鯊 Android 11 + ASUS ZE601KL 升 6.0.1）把「軟體＋網路＋多機協作」能驗的全清了（G1/G5身/G2 分散式兩台真機 PASS、必測④ 雙機 socket、unicast 備援）。**剩下沒有「再寫程式就能解」的卡關**，只剩：

### 🔧 只有「真 Kebbi」能測（兩台 Android 無法替代）
- **必測③** `move`/`turn` 是否被 NuwaSDK 授權牆擋
- **必測⑤** DOA 聲源方位解析度／正後方（需麥克風陣列；SimBody DOA=0）
- **必測①** 搶麥（系統持麥時 AudioRecord 能否取 PCM，凱比特有情境）
- **必測⑥** `ctlMotor(度,速)` 語意 ＋ NeckZ／各關節實際可達角度現場量測
- shoulder_y「舉手=100」超過官方 max70 的零點/正負 remap（量測後才敢改；見 [[kebbi-sdk-hardware-specs]]）
- 真機版 `kebbi-arm64.apk` 裝真 Kebbi 跑（馬達實動/多機）

### 👂 需「人」（耳朵/真人）
- TTS 印尼語**發音品質**（人耳聽喇叭）
- 真人講印尼語的 **STT 辨識準確度**（自動測只能驗靜音=空）
- **G5/G2 遠端語音**是否真從被控機喇叭播出（UnityVoice 不印 log，需人耳；兩台真機可現場驗）
- 相機權限（理論可測但 app 未用相機，且要人看畫面）

### 🧑‍💼 需「你」決策/給資料
- 各隊初賽教案「主攻」確認 → 才能定稿 5 教案 ＋ 決賽腳本
- 合併 `auto/dev-loop` → `main`（對外 push 需點頭）
- 輪替先前外洩的 Azure/OpenAI key

---
### 📜 已解除的明細（保留證據，新到舊）
- ~~**階2 單機整合測試（語音/LLM/UI）**~~ ✅ **PASS(2026-06-19,實機自動測,免互動)**：在接上本機的 Android 手機(MIUI `DLT_A0`)上,用 env 注入金鑰的中介版 APK 實測:app 起得來不崩、`[Kebbi] Mode=G4_TebakArah` 無「未指定 secrets」警告(=金鑰已進 APK)、`[G4] 校準學生A heard=""`(TTS 印尼語有出、STT 有收音回空=無學生屬正常)、走完 ForwardRound(答錯觸發 LLM tip,無例外)、`結算:答對0/1`(無學生屬正常)。→ **TTS/STT/LLM 三條雲端管線在真手機上全跑通**(金鑰有效;cloud-test 亦已驗 TTS→STT 辨識"kanan"、LLM 回印尼語 tip)。
  - **權限免彈窗**:`adb install -r -g <apk>` 安裝時自動授予所有 manifest runtime 權限(RECORD_AUDIO/INTERNET/MODIFY_AUDIO 已 granted);app 未宣告 CAMERA(本 build 無 G3 視覺)。
  - 📝 **仍需你互動驗**:① TTS 印尼語**發音品質**(要人耳聽手機喇叭) ② 真人講印尼語的 **STT 辨識準確度**(自動測只能驗靜音=空)。
  - 🔧 **仍需硬體**:真 Kebbi(馬達/DOA/搶麥/move——本 middleware 用 SimBody 故 DOA=0);必測④雙機 UDP 收端需第 2 台裝置。
- ~~**必測④ 雙機收送**~~ ✅ **雙向 PASS(2026-06-19,實機自動測,用 Mac 當 LAN peer)**：出了 `LinkPingTest` 變體 APK(`build-apk.sh linkping`,`kebbi-linkping-arm64.apk`),裝在手機(192.168.1.108)上 `Mode=LinkPingTest`。**送**:手機廣播 `Kebbi-A\x01*\x01ping #N`→同網段 Mac(192.168.1.114)收到 9/10 個封包(UDP 掉 1 正常)、framing 正確。**收**:Mac 廣播 `Mac\x01*\x01hello-from-mac`→手機 app log `[Link] ✅ 收到 Mac: hello-from-mac`(UnityRobotLink 收得到、RobotLinkProtocol 解框正確、OnMessage 觸發)。→ 整條多機傳輸層(G1Director/Controlled/finale/遠端語音)底層已驗。**⚠️ 後續修正(2026-06-19 兩台真機)**:當時 Mac 收得到手機廣播,但換另一個 WiFi 接兩台手機時發現**該 AP 會丟手機↔手機的廣播幀**(unicast 通、broadcast 不通) → 已加 **unicast 直連備援**(`PeerRegistry`+`UnityRobotLink`,見 DONE 第一條),真機驗過備援可達。**結論**:廣播能不能通**看現場 AP**,上線一律設 `KEBBI_PEER_IP` 走 unicast 較保險。**剩**:兩台都跑 app 的完整遊戲(需 Android 5.1+ 第二台)。
- ~~**多機 G1 分散式實跑(中控發送鏈)**~~ ✅ **PASS(2026-06-19,實機自動測,Mac 解碼驗證)**：出 `G1Director` 變體 APK(`build-apk.sh g1director`),手機跑 `Mode=G1Director` → RelayQuestGame 驅動本機 body A(Sim)+ HANDOFF + 遠端 body B(RemoteBodyProxy 經 UDP)。Mac 收到並解碼**正好 7 個 BodyCommand**(`Kebbi-A→Kebbi-B`:Move0.1→Stop→Turn-30→Move0.1→Stop→SetMotor RShoulderY=100→LShoulderY=100),完全對應 B 在 HANDOFF 後的走位(FWD/LEFT/FWD)+ 終點舉雙手。→ **中控驅動遠端機的整條鏈(遊戲→RemoteBodyProxy→BodyCommand→UDP)在實機正確**。
- ~~**被控機 Controlled 收命令執行**~~ ✅ **PASS(2026-06-19,Mac 當中控送、手機 Controlled 執行)**：出 `Controlled` 變體 APK,手機跑 `Mode=Controlled`;Mac 廣播 framed `BC|`(target=Kebbi-A) → 手機 `BodyCommandReceiver` 收下並套到 SimBody:`🦾 RShoulderY→100°`(BC\|SM\|4\|100\|50 收到+解碼,motorId 4=RShoulderY 證實 KebbiMotor==SDK ID+SetMotor 執行)、`BC\|MV/TN` 收到並 Move/Turn(此 build 為 H201 無底盤故 log「無底盤」,命令路徑通)。→ **分散式兩端在實機都驗過(送+收執行)**。**剩**:真正兩台一起跑完整遊戲(+真 Kebbi 有輪子讓 Move 實際走);G2/G5 比照。
- **真凱比實機**（馬達/DOA/搶麥/多機 UDP）：裝 `kebbi-arm64.apk`（真機版）。`ctlMotor` 的(度,速)語意待量測(必測⑥)；NeckZ 等關節實際可達角度待量（目前 src 用 ±90° 佔位）。
- **多機 G5/G2 遠端語音實跑**：`RemoteVoiceProxy` 邏輯/協定已自測綠;實機需在 `KebbiAppBehaviour` 補 `G5Director`/`G2Director` Mode(把 defVoice→RemoteVoiceProxy)並在兩台手機/實機驗:被控機是否真用自己喇叭說出台詞。先過必測④雙機收送。
- ~~遠端語音「說完才交棒」ACK 握手~~ ✅ **已解**(RemoteVoiceProxy 等播畢模式 + BodyCommandReceiver ackVoiceDone + VC|DONE,已自測延遲語音 await 等到/逾時不卡死)。真機剩餘:被控端的 `IVoice.SpeakAsync` 需真的等到「喇叭播放結束」才回(UnityVoice 目前 Play() 後即返回 → 要嚴格等播畢需改成等 AudioSource 播完),以及 UDP 傳輸必測④。
- ~~合體彩蛋跳過判定改 await 逾時~~ ✅ **已解**(LinkAwaiter 的 await ACK/DONE 帶逾時,Sim 與真機 UDP 非同步皆正確,已加非同步路徑自測)。真機剩餘只有 UDP 傳輸本身(必測④)與逾時值現場校準(預設 ack 1000ms/done 2500ms,視 WiFi 延遲調整)。
- 各隊初賽教案草稿 → 需先確認各隊主攻是否照建議

## ❓ 需要的資訊（你幫我找/決定，填好我就接著做）
- **[第二台 Android 5.1+ 裝置]** 目前接的第二台 ASUS ZenFone 2 是 **Android 5.0.2（API 21）< APK minSdk 22**（Unity 2022.3 硬下限），跑不了我們的 app，只能當網路接收端。**要兩台都跑 app 的完整多機遊戲測試（G1 接力／G5 辯論／G2 中控-被控、兩端真機身/語音），需要一台 Android 5.1 以上的第二機**（或把 ASUS 升到 5.1/6.0，但我無法安全遠端刷機）。網路層 unicast 備援已用 ASUS 驗過可達。
- **[實機實測]** 在輪式 Kebbi/工作坊現場驗：① 搶麥（系統持麥時 AudioRecord 能否取 PCM）② 相機權限（Unity WebCamTexture 能否開）③ `move`/`turn` 是否被授權牆擋 ④ 雙機 socket 收送 ⑤ DOA 解析度/正後方
- ~~**[SDK 數值]** NeckZ 與各關節實際可達角度範圍~~ ✅ **已提供(2026-06-19,官方 `NUWA_Robot_SDK_en.html` 馬達角度表)**：見 [[kebbi-sdk-hardware-specs]]。**NeckZ 真實 ±40°(非 ±90)**→ 已改 `SimKebbiBody`/`RealBackends`。各關節 Min/Max：neck_y±20 / neck_z±40 / shoulder_z −85~5 / shoulder_y −200~70 / shoulder_x −3~100 / 肘 −80~0；move±0.2m/s、turn±30°/s。**含意**：實機頭轉不到 ±90 → 無法只靠轉頭面向正右/正左(90°)的學生(只 Depan/serong 可達)，更側邊需轉底盤或接受部分面向(程式已降級)。**待**：shoulder_y「舉手=100」超過 max70，零點/正負向語意不明 → remap 需實機量測後再動。
- ~~**[決策]** git init + commit 把 robot_app 版本化~~ ✅ **已確認版本化(2026-06-19)**：robot_app 早已在 git（`auto/dev-loop` 分支，本 session 多個段落逐一 commit，44 檔全 tracked、working tree clean）。**合併到 `main` 仍依慣例由你決定**（要的話我可 `git merge auto/dev-loop` 進 main；推到 origin 屬對外、需你點頭）。
- ~~**[決策]** LLM provider~~ ✅ **已定(2026-06-19)**：**續用 OpenAI `gpt-4o-mini`**；若實測不夠力再升級(屆時記回此處)。
- **[內容]** ~~確認後我寫初賽教案草稿~~ ✅ **草稿 v1 已寫(2026-06-19)**:`教案_G1~G5_*.md` 五份,依建議主攻、用大會初賽 9 項格式(科目/對象/目標/成果/課綱素養/架構圖/Kebbi 教案設計/回饋/參考)、對焦應用35創意35挑戰30、各標「只有機器人做得到」、連結已實作程式。**待你**:確認/微調各隊主攻(G1 雙機接力/G2 幾何證明/G3 鏡像教練/G4 印尼語/G5 法庭辯論)→ 我據以定稿。
- **[階2 環境]** ~~Unity 2022.3 是否已裝好、能出 Android APK?~~ ✅ **已確認(2026-06-19)**:KebbiBrainUnity 2022.3.62f3 已能出雙 APK。**剩**:你手邊有幾台可同網段(同 WiFi)測試的 Android 手機?(多機 UDP 互連測試需 ≥2 台真手機;模擬器多半不通)。
- **[安全]** 先前不慎外洩的 Azure / OpenAI 金鑰記得 rotate
