# 進度追蹤（自主開發 loop）

> 我自己盤點「能獨立完成」的功能逐一做完；需要你提供的資訊集中在最下方「❓ 需要的資訊」，你填好我就接著做。
> 最後更新：2026-06-19（loop 進行中；自測 **344/344 綠**。**純 Sim 高價值功能已全數收割（功能完成）**：本 session 連收 G1 Level3 / G3 動作幀 HoldMs·Loops / G2 甲機轉頭 NeckZ / G4 八向方位+最近可達扇區。**Unity 編譯/出 APK 已 DONE**（使用者另一 session 證實：`~/Projects/KebbiBrainUnity` Unity 2022.3.62f3，Assets/Scripts 以 **symlink** 接 robot_app/src 的 App/Hardware/Real/Sim、Config/KebbiFactory 為實檔、Cloud/ 刻意不接；`csc.rsp -define:UNITY` + `Editor/KebbiBuild.cs` 的 BuildApk/BuildMiddlewareApk → IL2CPP/ARM64 出 `Build/kebbi-arm64.apk` 與 `kebbi-middleware-arm64.apk` 各 ~9.5MB，含 src/Real AOT）。**剩餘高槓桿全在實機驗證（BLOCKED）**：把 `kebbi-middleware-arm64.apk` adb install 到手機跑 TTS/STT/LLM/UI、必測④雙機 UDP、馬達/DOA。詳見最下方「⏳ BLOCKED / ❓需要的資訊」）

---

## ▶ 如何接續（/compact 或新 session 後，讀這一段就能繼續）
**這份檔就是 backlog ＋ handoff。接手步驟：**
1. **環境**：.NET SDK 8 在 `~/.dotnet`（未進 PATH）。先 `export PATH="$HOME/.dotnet:$PATH"`。程式在 `robot_app/`。
2. **驗證現況**：`dotnet run --project robot_app/KebbiBrain.Sim.csproj -- --test`（目前應 **344/344 綠**；本機只有 .NET 10 → 需 `DOTNET_ROLL_FORWARD=Major`，見 [[dotnet-env-quirk]]）。
   各 demo：`--g1`（接力闖關）`--g2`（幾何接力）`--g3`（鏡像教練）`--g5`（法庭辯論）`--link`（多機）`--rv`（多機遠端語音）`--finale`（合體彩蛋接力大舞台）；G4 預設 demo；`--cloud-test`／`--target cloudsim`（需 Azure/OpenAI 金鑰）。
3. **挑下一項**：做「🔄 DOING」那項；沒有就做「📋 TODO」最上面一項。
4. **做法（沿用現有架構，勿另起爐灶）**：在 `robot_app` 用抽象介面 `IKebbiBody / IVoice / ILlm / IRobotLink / IPoseSensor` ＋ `Sim` 後端實作新邏輯 → 加自測（`Tests.cs`）→ build＋`--test` 全綠 → 把該項從 DOING/TODO 移到 ✅DONE、更新本檔。
5. **金鑰**：Azure（語音）＋OpenAI（LLM）已設為環境變數 `KEBBI_SPEECH_KEY`/`KEBBI_SPEECH_REGION`/`KEBBI_LLM_KEY`（provider 依金鑰前綴自動選）。⚠️先前外洩的金鑰記得 rotate。
6. **背景知識**：競賽/SDK 實測/骨架細節見 `~/.claude/projects/-Users-dave-chen-Projects-kebbi-brain-storm/memory/` 的 `kebbi-*.md`（每次 session 自動載入索引）。其他文件：`開發平台_可行性評估.md`、`技術實作路線_各隊.md`、`隊伍_G*.md`。

**接續 prompt（複製給我即可）**：
> 讀 `進度追蹤.md` 的「▶ 如何接續」，接手把 DOING／TODO 最上面還沒做完的做完，加自測跑綠，並更新該檔。

---

## 🧪 測試策略：三階（使用者 2026-06-18 定）
**核心：以 Android 為模擬器、Unity 為 middleware，在 Android 上跑「所有功能」測試（含多機互連），最後才上真凱比。**

| 階段 | 平台 | 機身(馬達/DOA) | 語音/LLM | 多機 | 用途 | 狀態 |
|---|---|---|---|---|---|---|
| **階1 主控台 Sim** | dotnet console | 模擬(文字) | Sim/真雲端(CloudSim) | 行程內 loopback | 邏輯規格、CI 自測 | ✅ 344/344 綠 |
| **階2 Android 中介** | 一般 Android(模擬器/手機,**非凱比**) + Unity middleware | **模擬**(SimKebbiBody) | **真** Azure 印尼語 + 真 LLM | **真 UDP**(UnityRobotLink) | 在 Android 上整合測試「除馬達/DOA 外的所有功能、含多機 UDP 互連」 | 🔄 程式已備(見下),待你在 Unity/Android 跑 |
| **階3 真凱比** | Kebbi(Android+NuwaSDK) | **真**(UnityKebbiBody) | 真 | 真 UDP | 上線實測(馬達/DOA/搶麥/相機) | ⏳ 需工作坊/實機 |

**階2 怎麼切**：Unity build 設 `Config.Target=Real` + `Config.UseRealRobotApi=false`（或 KebbiAppBehaviour 取消勾 `useRealRobotApi`）→ KebbiFactory 自動把機身換成 `SimKebbiBody`，語音/LLM/UDP 仍走真的。如此**不需要真凱比，就能在一般 Android 手機/模擬器把整條流程(含多機)跑起來測**。
- **多機互連測法**：2~3 台**真 Android 手機**接同一 WiFi，各跑 app 設不同 `RobotId`、`Mode=LinkPingTest` → 互收 ping 即 OK；再換各隊多機遊戲。
  ⚠️ **Android 模擬器(AVD)彼此網路隔離(NAT),UDP 廣播多半互通不到** → 多機測試請用真手機同網段;模擬器僅適合單機功能(語音/LLM/UI)測。
- **DOA 在階2**：無真麥陣列 → 用螢幕按鈕/腳本設定 `SimKebbiBody.CurrentDoa` 餵入(待補簡易測試 UI)。

---

## ✅ DONE
- 競賽研究（114 辦法/評分/時程）＋ 5 隊選題 ＋ 各隊獨立手冊 ＋ 點子庫 ＋ 合體彩蛋
- 開發平台可行性評估（CodeLab/RaaS/Unity）＋ SDK 實測整合（無移動/授權牆/DOA/關節…）
- 各隊技術實作路線（對抗式審查）＋ 印尼語雲端方案 ＋ 金鑰申請步驟
- `robot_app` 骨架：硬體抽象 ＋ 文字模擬器 ＋ `Sim/CloudSim/Real` 三層切換（LangVersion 9.0，可搬進 Unity）
- **G4《Tebak Arah》切片**：遊戲邏輯 ＋ 自測 ＋ **真雲端驗證通過**（Azure 印尼語 TTS/STT ＋ OpenAI gpt-4o-mini）
- **G3《鏡像體操教練》切片**：關節逐幀示範 ＋ DOA 轉頭調節奏 ＋ 姿態感測抽象（IPoseSensor/SimPoseSensor）＋ `--g3` demo
- **多機抽象 `IRobotLink` ＋ 雙機模擬**：行程內 loopback 匯流排（SimRobotBus/SimRobotLink）＋ Unity 實機桩 ＋ `--link` demo（雙機交棒握手 + 彩蛋廣播）
- **G2《幾何證明接力站》Sim 切片**：雙機說—走—指—接棒（GeometryRelayGame）＋ `--g2` demo（3 步等腰證明）
- **G5《法庭辯論劇場》Sim 切片**：雙機事件交棒接力辯論 ＋ 中央逼近(移動) ＋ DOA 轉向發言學生 ＋ 手臂指控/攤手（DebateGame）＋ `--g5` demo（伽利略審判）＋ 自測（共 42/42 通過）

- **G1《雙機接力闖關》Sim 切片**：指令序列直譯 → 雙機地板接力（RelayQuestGame）＋ `--g1` demo ＋ 自測（共 47/47 通過）。**至此五隊 G1–G5 Sim 切片全到齊**。
- **🎯 Unity 實機後端（RealBackends.cs，真 UnityWebRequest）**：UnityKebbiBody（NuwaRobotAPI）＋ UnityVoice（Azure 印尼語 TTS/STT，含 WAV↔AudioClip）＋ UnityLlm（OpenAI/Anthropic 自動選）＋ UnityRobotLink 桩 ＋ UnityWebRequest→Task 工具。形狀照 --cloud-test 已驗證的 REST。
- **`UNITY_接入指南.md`**：Unity 專案建立、SDK 匯入、`UNITY` 編譯符號、金鑰注入、MonoBehaviour 入口範例、build/部署(adb)、6 項上線必測。
- **🎯 Unity 後端三件套(實檔,皆 `#if UNITY`)**：
  ① `src/Real/UnityRobotLink.cs`——真 **UDP 廣播**收送(同埠廣播 + 收端過濾;網路緒收到 → 切主緒回呼);
  ② `src/Real/KebbiAppBehaviour.cs`——場景入口 MonoBehaviour,兩種單機模式 `G4_TebakArah` 與 `LinkPingTest`(後者就是必測④雙機收送的現成工具);
  ③ `src/Real/KebbiSecrets.cs`——金鑰注入用 ScriptableObject(`Assets→Create→Kebbi/Secrets`,`.asset` 記得加 .gitignore)。
- **🔌 多機協定抽出可測層 `src/Hardware/RobotLinkProtocol.cs`**：把實機 UDP 的「框架/解析/收件判斷」抽成純 C#,UnityRobotLink 直接用它 → **這段邏輯已在主控台自測(10 項,92/92)**;真正剩未驗的只有 UDP 傳輸本身(必測④)。
- **🧪 階2「Android 中介測試」接點(程式已備)**：`Config.UseRealRobotApi`(預設 true)；`KebbiFactory` Real 分支依此二擇一機身(true→UnityKebbiBody / false→SimKebbiBody)；`KebbiAppBehaviour.useRealRobotApi` 勾選同步。→ 設 false 即可在一般 Android 手機/模擬器跑真語音/LLM/UDP、模擬機身。console build 0/0、92/92 不受影響。
- **🕹️ 真機多台分散式控制層(純 C#,已自測 8 項)**：`src/Hardware/` 下 `BodyCommand`(機身動作↔文字命令)＋`RemoteBodyProxy`(IKebbiBody:每個寫動作經 link 送命令給另一台執行;讀回最後下達值)＋`BodyCommandReceiver`(被控機:收命令套到本機 body,非命令訊息轉交)。→ **一台中控用既有 G1/G2/G5 遊戲邏輯(bodyB 換成 RemoteBodyProxy)即可同時驅動本機＋遠端機,程式不改**。Sim 端用 SimRobotBus 全程自測過;實機只剩 UDP 傳輸(必測④)。
- **🎮 多機 Unity 入口(KebbiAppBehaviour 新增兩模式,`#if UNITY`)**：`G1Director`(本機當中控跑既有 G1 遊戲,遊戲內部 A↔B 握手走本機 loopback bus、只有機身命令出 UDP 到被控機)＋`Controlled`(本機當被控,只掛 BodyCommandReceiver)。→ 一台 G1Director＋一台 Controlled(robotId 不同、peerRobotId 互指)即可跑分散式多機。G2/G5 比照(換各自建構式)。console 0/0、92/92 不受影響。
- **✏️ 主控台打磨**：`--menu`／`--help` 命令清單 ＋ 邊界自測 27 項(方位扇區邊界 135/-135/180/-180、角度正規化 270/-270/360、印尼語詞往返、轉頭夾限邊界 90/-90/91/200、機身命令負值與速度參數)→ 當時共 **92/92**。
- **🗣️ 多機「遠端語音」層 `RemoteVoiceProxy`／`VoiceCommand`(純 C#,已自測 22 項 → 共 114/114)**：機身三件套的語音版 —— `VoiceCommand`(線格式 `VC|SAY|lang|text`,限數量切割保留台詞內的 '|'、lang 防呆、lang 預設值過 wire 正規化)＋`RemoteVoiceProxy`(IVoice:`SpeakAsync` 經 link 送 VC 命令讓「被控機用自己的喇叭」說;`ListenAsync` 不支援遠端聽→回空字串並 log 警告)＋`BodyCommandReceiver` 擴充(加 `IVoice localVoice`,分派鏈 BC→VC→alsoHandle)＋真機 `KebbiAppBehaviour.RunControlled` 接上 `ctx.Voice`。→ **G5 辯方/G2 乙機 Director 模式下,把 `defVoice` 換成 `RemoteVoiceProxy` 即可讓對方機「自己開口」,DebateGame 程式不改**。經設計階段 3 視角對抗式審查(抓到 IndexOutOfRange/lang 不過 wire/Split 多載可攜性等 8 項並修正)。⚠ `SpeakAsync` 合約降級:送出即返回不保證播畢(真機 UnityVoice 本就如此),真機嚴格交棒需補 ACK/DONE 握手(見 BLOCKED)。
- **🎮 示範小遊戲 `--rv`**：跑「未改一字」的 DebateGame,但辯方=遠端被控機 → 辯方台詞與手勢由被控機自己的喇叭/機身執行(主控台可見 `VC|SAY`/`BC|*` 命令越過 loopback「網路」抵達被控機)。實機把 SimRobotBus 換成 UnityRobotLink(UDP)即同款。
- **🎪 合體彩蛋《凱比聯合學園祭》編排器 `FinaleShowGame`(純 C#,已自測 15 項 → 共 129/129)**：對應 `合體彩蛋_G2G3G5_多機協作.md`。中控導演機依序 cue 各關卡站(G2 幾何/G3 律動/G5 對峙,`CUE|role`)→ 站台回 `ACK|role`(開始)/`DONE|role`(完成)→ 中控收 DONE 才往下;壓軸廣播 `FINALE` → 全體走位中央 + 同步勝利動作(中控也舉手)。**★降級備案(doc 標「最重要」):站離線(無 ACK)或卡住(ACK 無 DONE)→ 中控自動跳過,其餘照跑、壓軸照常 → 合體翻車不拖垮全場**。自測涵蓋:三站全到/一站離線/一站卡住/全站離線四情境。
- **🎮 示範小遊戲 `--finale`**：三場——①三站全到的完美接力(壓軸全體舉手到 100°);②G5 夥伴機臨時離線 → 中控自動跳過、壓軸照跑;③G2 站非同步延遲回應 → 中控 await 等到才算完成。主控台可見 `CUE/ACK/DONE/FINALE` 訊息流。
- **🎉🎉 用 Unity 出了一個「合規可裝機」的 Kebbi Android APK(整條 build 鏈打通)**：在 `~/Projects/KebbiBrainUnity` 寫 `Assets/Editor/KebbiBuild.cs`(設 arm64/IL2CPP/minSDK22/targetSDK33/package=com.kebbibrain.app,掛 `KebbiAppBehaviour` 防 strip 確保 src/Real 進 AOT)→ batchmode `-executeMethod KebbiBuild.BuildApk`。**RESULT=Succeeded、0 錯 0 警告**,產出 `Build/kebbi-arm64.apk`(9.6MB)。aapt 驗證:`native-code: arm64-v8a`(無 armv7)、`sdkVersion 22`、`targetSdkVersion 33`、`lib/arm64-v8a/libil2cpp.so`(10MB,IL2CPP 把全部 managed 含 src/Real 編成原生)、NuwaSDK aar 已合併(manifest 帶入 BLUETOOTH 權限)。→ **robot_app 從 Sim 邏輯(153/153)→ #if UNITY 後端 → IL2CPP arm64 原生 → 合規 APK 整條鏈驗通**;剩下只差實機安裝執行。**出了兩支 APK**:`kebbi-arm64.apk`(實機版 useRealRobotApi=true,只跑真 Kebbi)＋`kebbi-middleware-arm64.apk`(中介版 useRealRobotApi=false → 可跑一般 Android 手機/模擬器測語音/LLM/UI,階2 策略)。build 指令(`KebbiBuild.BuildApk` / `BuildMiddlewareApk`):需 sandbox 關、~9 分。⚠️ **第二次 batchmode 開啟同專案會卡死 → 每次 build 前 `rm -rf Library Temp Logs`**(見 memory)。
- **🎉 `src/Real` 整段 `#if UNITY` 後端在真 Unity 編譯成功(清掉「這台無 Unity 無法編譯」的最大 BLOCKED)**：建獨立 Unity 專案 `~/Projects/KebbiBrainUnity`(2022.3.62f3,**非** UnityKebbi 參考專案)→ 把 `robot_app/src` 的 Hardware/Sim/App/Real/Config/KebbiFactory 以 symlink 接進 `Assets/Scripts`(單一來源)+ `Assets/csc.rsp` 開 `-define:UNITY` + NuwaSDK aar 進 `Assets/Plugins/Android` → batchmode 編譯(buildTarget Android)。結果 **0 error / 0 warning**,`Library/ScriptAssemblies/Assembly-CSharp.dll` 含全部 9 型別(UnityKebbiBody/UnityVoice/UnityLlm/UnityRobotLink/KebbiAppBehaviour/KebbiSecrets + RemoteVoiceProxy/FinaleShowGame/LinkAwaiter)。→ **先前只能「對 aar 反查」的 `#if UNITY` 程式現在真的在 Unity 編過了**(含 `ctlMotor` 修正);robot_app/src 內的 `.meta` 已 gitignore。重編指令見下方 BLOCKED 區。剩下真機驗證(出 APK 裝機跑)仍需實機。
- **🔩 src/Real 馬達 API 真機 bug 修正(對真 NuwaSDK 2.1.0.08 aar 反查,自測 +10 → 153/153)**：用參考專案 `~/Projects/UnityKebbi` 內附的真 aar 以 javap 反查 `NuwaRobotAPI` 實際 public 方法,發現 `UnityKebbiBody` 兩個方法名是錯的、實機會丟 NoSuchMethod:① 設角 `setMotorPositionInDegree` **不存在** → 真 API 是 `ctlMotor(motorId,度,速)`;② 讀角 `getMotorPresentPossitionInDegree` **拼字錯** → `getMotorPresentPositionInDegree`(單 s)。`getInst()/move/turn/getDirectionOfDOA` 反查確認**正確**。馬達 ID:`KebbiMotor` enum 值 == SDK `MOTOR_*` 常數(NECK_Y=1…LEFT_ELBOW_Y=10),已加純 C# `T_NuwaMotorIds`(10 項)在主控台釘住。→ 這是「只有對著真 SDK 才驗得出」的實機 bug,靠 UnityKebbi 當參考解掉。`ctlMotor`(度,速)語意仍待實機量測(必測⑥)。
- **🗣️ 遠端語音「說完才交棒」握手 `VC|DONE`(純 C# 已自測 6 項 → 共 143/143)**：`VoiceCommand` 加 `Done()`/`IsDone()`/`TryParseSay()`;`RemoteVoiceProxy` 加「等播畢」模式(傳入 `LinkAwaiter` → `SpeakAsync` 先註冊 DONE 等待者再送 SAY、await 被控機回 `VC|DONE` 帶逾時才返回);`BodyCommandReceiver` 加 `ackVoiceDone`(收 VC|SAY → 等本機 voice 播畢 → 回 `VC|DONE`)。→ **G5 DebateGame「辯方說完才換控方」在真機也正確**(把 defVoice 換成等播畢的 RemoteVoiceProxy、被控端設 ackVoiceDone)。預設仍 fire-and-forget(向後相容)。自測:延遲語音證明 await 真等到播畢、fire-and-forget 對照、逾時不卡死。`--rv` 加「後記」場景示範 VC|DONE 回流。解決 cycle 1 review 標的真機 TODO。
- **🔗 `LinkAwaiter`(IRobotLink 上的 await+逾時 request/response 助手,純 C# 已自測 6 項 → 共 137/137 時)**：提供 `WaitForAsync(predicate, timeoutMs)`,用 TaskCompletionSource + Task.Delay,「先註冊等待者再送觸發訊息」→ **Sim 同步遞送與真機 UDP 非同步遞送皆正確**。**用它把 FinaleShowGame 的跳過判定從「送 CUE 後同步讀旗標(只 Sim 成立)」改成「await ACK(逾時→離線)/await DONE(逾時→卡住),且只接受 from==該站」** → 解決兩輪 review 同列的 #1 真機問題(舊版真機會每站誤判離線)。自測新增「站台延遲後才回 ACK/DONE」的非同步路徑案例,證明 await 真的等到(非舊同步路徑)。經 2 視角對抗式 review(並發正確性 + 真機)。

## 🔄 DOING（本回合）
- （無）Unity 後端三件套本回合已寫完並串接;Unity 端真正驗證為 BLOCKED(這台無 Unity)。

## 📋 TODO（我可獨立做）
1. 〔階2 支援・#if UNITY 待實機驗〕KebbiAppBehaviour 加「螢幕測試 UI」(按鈕設模擬 DOA、選隊伍、顯示 log)。
   - ~~對方機要「自己說話」需 `RemoteVoiceProxy`~~ ✅ **已完成**(協定/分派/被控端接收/真機接點皆已做、22 項自測綠、`--rv` 示範)。
     **剩未做(#if UNITY,待實機)**:在 `KebbiAppBehaviour` 新增 `G5Director`/`G2Director` 兩個 Mode(比照既有 `G1Director`,把 `defVoice` 換成 `RemoteVoiceProxy`、`defBody` 換成 `RemoteBodyProxy`)→ 讓 G5/G2 也能實機多機跑。此為 Unity 入口接線,本機無 Unity 無法編譯驗證 → 歸 BLOCKED。
2. 各隊「初賽 3 頁教案格式」草稿（純文件；**待主攻確認**才動）。

### 🚀 平行研究+平行實作（使用者 2026-06-18「多個功能平行同時研究」）
6 agent 平行研究產出可實作規格 → 4 個「動的遊戲檔不重疊」功能用 4 worktree agent **平行實作**(各自 build+test 綠)→ 主迴圈整合(零衝突)。一次 191→273/273:
- **G5 七步審判驅動器**(`RunTrialAsync`:開庭→陳述→交棒→反駁→逼近→轉向學生→結辯)+ **學生席舉手插話分支**(姿態 gate→轉頭→學生麥→改票);`--g5t`;+22 測試。
- **G4 裁判賽**(A 描述 B 方位、Kebbi 用 DOA 真值核對計分)+ **視角轉換**(`Direction.RelativeDir`:我的右=Kebbi 的左)+ **多輪排名**(`RunTournamentAsync`/Ranking);`--g4t`;+12 測試。
- **G3 逐幀回退**(`CurrentFrame` 索引 + `RewindOneFrame`/`HandleAgainAsync`:喊「再一次」回退一幀重示範);`--g3r`。
- **G2 學生自編走位腳本驗證器**(`ValidateScript`:缺步/多步/層次錯置/順序錯→指出第幾步哪種錯,通過才接力);`--g2v`;+11 測試。
- **(序列批)G1 交棒「換你」語音+舉手手勢**(建構式加可選 voiceA/voiceB:A 舉手「換你！」→ B「收到，換我！」→ A 放手) + **G1 交接點同步條件**(LevelMap 加 'H' 交接點 + Level2 轉彎走廊;A 沒走到 H 就 HANDOFF=交棒失敗 B 不啟動);+9 測試;`--g1` 加交棒演出 + 關卡2。→ 平行研究的 6 功能全部完成(273→282/282)。

### 📦 純 Sim 遊戲深度 backlog（多代理 survey 2026-06-18；皆本機可驗、可配 demo，供 loop 逐項收割）
> 結論修正：先前寫「高價值項見底」**不再成立**——五隊遊戲性盤點出 ~32 個純 Sim 缺口。三條高槓桿主軸:① 寫死腳本→資料化關卡庫＋場次驅動器(RunSession/關卡 Map);② 補 Score/PrintSummary 結算(G1、G5 尚缺);③ 把剩餘同步讀旗標遷到 LinkAwaiter(G2)。
- **G1（最薄,優先）**：~~①關卡地圖+障礙/避障條件積木~~ ✅ **已做**(commit 見下,`LevelMap`+`RelayQuestGame` 加地圖/朝向/撞牆 Crashed/`IF_OBSTACLE`/`IF_CLEAR`/`Reset` 可重入,9 項自測,`--g1` 三回合 demo 撞牆→繞行→智慧避障;無地圖=舊行為向後相容)。 ~~②闖關計分/星等 PrintSummary~~ ✅ **已做**(commit 見下,Attempts/TotalCrashes/Stars 星等[以 BFS 最短路徑為基準]/PrintSummary;5 項自測;`--g1` 結算「第N次嘗試、累計撞牆、效率★★★」) ③~~失敗重試閉環 Reset~~(Reset 已做,可再加多份程式對比) ~~④難度分層多關卡~~ ✅ **已做**(Level1 一處障礙、Level2 轉彎走廊+交接點 H、Level3 多障礙 S 形強制路徑+交接點 H;`T_G1_Level3` 6 項自測:撞牆版撞 #(1,3)、正解 7 步==最短 3 星、可重入;`--g1` 加關卡3 demo) ~~⑤交棒點同步條件~~ ✅(沒走到交接點就交棒=失敗,Level2/3 皆驗) ~~⑥交棒「換你」語音+舉手手勢~~ ✅。**→ G1 純 Sim 缺口全清。**
- **G2**：~~①交棒握手改用 LinkAwaiter~~ ✅ **已做**(commit 見下,送 POINT→await DONE 帶逾時,甲機逾時降級 StepsSkipped、可重入;3 項自測;`--g2` 加離線降級場景)。 ~~②學習單(已知/因為/所以)學生作答驗證+糾錯~~ ✅ **已做**(commit 見下,Step.Layer 可選 + 乙機問層次/學生答/Score/答錯提示;MakeIsoscelesProofWorksheet;4 項自測;`--g2` 加學習單;無 Layer 向後相容) ~~③多回合 session+結算~~ ✅ **已做**(commit 見下,RunSessionAsync 跑題庫多題+累計步數/學習單得分+PrintSessionSummary;4 項自測;`--g2` 跑完整題庫場次) ~~④證明題庫(目前只 1 條等腰)~~ ✅ **已做**(commit 見下,MakeAngleSumProof 內角和/MakeExteriorAngleProof 外角定理 + MakeProofLibrary 3 題具名;5 項自測;`--g2` 題庫換題) ~~⑤學生自編走位腳本 validator~~ ✅ **已做**(audit 核對:ValidateScript 覆蓋缺步/多步/層次錯置/順序逆序/缺標層 + RunProofIfValidAsync 驗證過才接力;`--g2v` demo)。~~⑥甲機轉頭望向發言者(NeckZ)~~ ✅ **已做(本輪)**(Step.TurnHeadToward 可選角度[可為負=望左],POINT 協定帶轉頭欄位;甲機收 POINT 先 SetMotor(NeckZ)轉頭視線跟隨再手臂指認;MakeIsoscelesProofTurnHead -45/0/45;`T_G2_TurnHead` 10 項自測[負角有效/0°望前/null 不轉/舊式 4 參數相容/可重入];`--g2h` demo;null=不轉頭向後相容)。**→ G2 純 Sim 缺口全清。**
- **G3（最成熟）**：~~①「再一次」回退一幀~~ ✅ **已做**(audit 核對:RewindOneFrame/HandleAgainAsync 純狀態回退+夾邊界+可重入;`--g3r` demo) ~~②多動作 session(太極/CPR)+語音切換下一組~~ ✅ **已做**(MakeTaichi/MakeCpr/MakeDefaultRoutine + RunSessionAsync;5 項自測;`--g3`) ~~③動作幀資料化(每幀停留/BPM/循環)~~ ✅ **已做(本輪)**(JointFrame.HoldMs 單幀自訂停留覆寫 BPM、Move.Loops 整組循環、FrameHoldMs/FramesPlayed 可純驗;`T_G3_Frame` 12 項自測;`--g3f` demo CPR 下壓停 2 秒+暖身 3 組+降 BPM 只影響未自訂幀;Loops=1 且無 HoldMs=等價舊版向後相容) ~~④左右鏡像對映~~ ✅ **已做**(MirrorMotor/MirrorMove;5 項自測;`--g3`)。**→ G3 純 Sim 缺口全清。**
- **G4**：~~裁判賽三角關係、視角轉換(RelativeDir)、排名(Ranking)~~ ✅ **已做**(audit 核對:JudgeRoundAsync/RelativeDir/PerspectiveRoundAsync/RunTournamentAsync/PrintRanking,12 項自測;`--g4t` demo)。~~八向方位升級~~ ✅ **已做(本輪)**(Direction 4 向→8 向[含斜向 serong kanan/kiri、belakang kanan/kiri,每區 45°];ParseIndo 複合詞優先解析;SectorCenterDeg/NearestReachable 最近可達扇區;KebbiHead.TurnToward 多載回報「夾限後實際面向扇區」=馬達可達性決定判決;`T_G4_EightWay`+邊界共 +25 項自測;`--g4e` demo;舊 4 向/3 參數多載完全向後相容,FromAngle 僅 G4+DOA log 消費,無他隊回歸)。
- **G5**：~~結辯投票計分~~✅ ~~學生發言分支(StudentInterjection)~~ ✅ ~~7 步驅動器(RunTrialAsync)細化~~ ✅ **全做齊**(audit 核對:RunTrialAsync 7 步 + HandleInterjectionAsync 姿態 gate→轉頭→學生麥→改票 + ConcludeAsync 宣判;`--g5t` demo)。候選下一功能:InterjectionScorer 插話加權計票(M,較大、較投機,優先度低)。
> 完整 32 項見 workflow 輸出 `tasks/wsxx7iwt8.output`。

> **🧭 loop 恢復點(2026-06-19,共 344/344 綠)**:已連收 4 個段落 — G1 Level3(`b3ec528`)、G3 動作幀資料化(`de7c66d`)、G2 甲機轉頭(`62a1ae7`)、G4 八向方位+最近可達扇區(`edd2cfb`)。**G1/G2/G3/G4 純 Sim 重點缺口全清**;G5 核心早已做齊。
>
> **🛑 純 Sim 開發判定「功能完成」(2026-06-19)**:最後候選 G5 `InterjectionScorer` 經讀碼判定**不值得做且該砍**——理由:`DebateGame.TrialScript.Interjections` 是 `Dictionary<int, StudentInterjection>`(key=回合索引),**結構上每回合至多一筆插話**;該候選的賣點(同一學生多次插話的「票數防灌水」、「幾位學生舉手的加權」)在現行模型**根本不會發生**,且 `Interjections` 計數已等同「被插話回合數」(每回合≤1)。要做它得先把插話模型改成「多筆/回合」=沒人要求的 scope creep。→ 結論:**純 Sim 高價值功能已全數收割**(三鏡頭提案+評審 wjgy32z4o 亦同此判)。
> **真正高槓桿全在 Unity/實機路徑**。✅ **Unity 編譯/出 APK 已 DONE**(2026-06-19 使用者證實:KebbiBrainUnity 2022.3.62f3、Assets/Scripts symlink 接 src 的 App/Hardware/Real/Sim、Cloud 不接、IL2CPP 出真機+中介雙 APK;見上「✅ 已排除」)。**剩餘=純實機驗證(BLOCKED)**:① 裝 `kebbi-middleware-arm64.apk` 跑 TTS/STT/LLM/UI ② 必測④雙機 UDP ③ 真凱比馬達/DOA/搶麥。loop 仍「等使用者實機/資料」模式:確認各隊主攻→寫教案;給 SDK 關節角度→替換 ±90 佔位;裝機跑→回報我接續。

> 方向（使用者 2026-06-18）：**專心做 Unity**，Blockly/CodeLab 太多功能做不到。Sim 層 = 已驗證的邏輯規格(5 隊 47/47)；接下來把 `src/Real` Unity 後端做實、做完整，並用 `UNITY_接入指南.md` 讓你能在 Unity 開起來。**Unity 端的真正驗證要在你的 Unity/實機上做（我這台沒有 Unity）。**

## ✅ 已排除（曾 BLOCKED，現已驗證解除）
- **Unity 後端編譯 + 出 APK**　✅ **DONE（2026-06-19 使用者另一 session 證實）**：`~/Projects/KebbiBrainUnity`（Unity **2022.3.62f3**）。`Assets/Scripts/` 下 **App/Hardware/Real/Sim 四個皆 symlink** 指向 `robot_app/src/*`（改 src 即時雙向同步），`Config.cs`/`KebbiFactory.cs` 為實檔；**`Cloud/` 刻意不接**（Unity 走 `Real/RealBackends.cs` 的 `UnityWebRequest`，Cloud 的 AzureSpeech/OpenAiLlm/Http 維持 console 專用）。`Assets/csc.rsp = -define:UNITY`；`Assets/Editor/KebbiBuild.cs` 提供 batchmode 入口 `BuildApk`（真機版 `useRealRobotApi=true`）/`BuildMiddlewareApk`（中介版 `false`）。**`Build/` 已產出 `kebbi-arm64.apk` + `kebbi-middleware-arm64.apk`（各 ~9.5MB，IL2CPP/ARM64 AOT 過、含整段 `#if UNITY` 的 src/Real）** → 證實 src/Real 在真 Unity 編得過、能出合規 APK。重編：`rm -rf ~/Projects/KebbiBrainUnity/{Library,Temp,Logs}` 後 `"$UNITY" -batchmode -quit -projectPath ~/Projects/KebbiBrainUnity -buildTarget Android -executeMethod KebbiBuild.BuildMiddlewareApk -logFile /tmp/kbu.log`（見 [[kebbi-autodev-loop]] 的 batchmode 第二次卡死坑）。

## ⏳ BLOCKED（需你提供資訊/實機後才能做）
- **階2 單機整合測試（語音/LLM/UI）**：程式與 APK 都備好。**下一步動作（需你）**：`adb install ~/Projects/KebbiBrainUnity/Build/kebbi-middleware-arm64.apk` 裝到一般 Android 手機/模擬器（中介版 `useRealRobotApi=false`，不需真凱比），跑起來看 ① 真 Azure 印尼語 TTS 出聲 ② STT 收音 ③ LLM 回覆 ④ UI/流程；**要有聲需先在 `KebbiSecrets.asset` 填金鑰再 build**（APK 金鑰是 build 時打包的，非環境變數；詳見 `UNITY_接入指南.md §7` 裝機 runbook）。把結果（或 `adb logcat` 錯誤）回報我。
- **必測④ 雙機收送（階2 用真手機）**：2 台 Android 手機同 WiFi，各掛 `KebbiAppBehaviour` 設 `Mode=LinkPingTest`、不同 `RobotId`，看 logcat 是否互收到 ping。⚠️模擬器(AVD)UDP 廣播多半互通不到 → 用真手機。結果回報我(成功/失敗/AP 擋廣播?)。
- **多機 G1 分散式實跑**：ping 通過後,一台設 `Mode=G1Director`(peerRobotId 指向對方)、一台設 `Mode=Controlled` → 看被控機 logcat 是否依命令動作(走格/舉手)。通過代表整條分散式控制鏈 OK,G2/G5 比照補。
- **真凱比實機**（馬達/DOA/搶麥/多機 UDP）：裝 `kebbi-arm64.apk`（真機版）。`ctlMotor` 的(度,速)語意待量測(必測⑥)；NeckZ 等關節實際可達角度待量（目前 src 用 ±90° 佔位）。
- **多機 G5/G2 遠端語音實跑**：`RemoteVoiceProxy` 邏輯/協定已自測綠;實機需在 `KebbiAppBehaviour` 補 `G5Director`/`G2Director` Mode(把 defVoice→RemoteVoiceProxy)並在兩台手機/實機驗:被控機是否真用自己喇叭說出台詞。先過必測④雙機收送。
- ~~遠端語音「說完才交棒」ACK 握手~~ ✅ **已解**(RemoteVoiceProxy 等播畢模式 + BodyCommandReceiver ackVoiceDone + VC|DONE,已自測延遲語音 await 等到/逾時不卡死)。真機剩餘:被控端的 `IVoice.SpeakAsync` 需真的等到「喇叭播放結束」才回(UnityVoice 目前 Play() 後即返回 → 要嚴格等播畢需改成等 AudioSource 播完),以及 UDP 傳輸必測④。
- ~~合體彩蛋跳過判定改 await 逾時~~ ✅ **已解**(LinkAwaiter 的 await ACK/DONE 帶逾時,Sim 與真機 UDP 非同步皆正確,已加非同步路徑自測)。真機剩餘只有 UDP 傳輸本身(必測④)與逾時值現場校準(預設 ack 1000ms/done 2500ms,視 WiFi 延遲調整)。
- 各隊初賽教案草稿 → 需先確認各隊主攻是否照建議

## ❓ 需要的資訊（你幫我找/決定，填好我就接著做）
- **[實機實測]** 在輪式 Kebbi/工作坊現場驗：① 搶麥（系統持麥時 AudioRecord 能否取 PCM）② 相機權限（Unity WebCamTexture 能否開）③ `move`/`turn` 是否被授權牆擋 ④ 雙機 socket 收送 ⑤ DOA 解析度/正後方
- **[SDK 數值]** NeckZ 與各關節「實際可達角度範圍」（官方 Unity readme 馬達角度表，或真機量測）→ 目前用佔位 ±90°
- **[決策]** 要不要 `git init` + commit 把 robot_app 版本化？（我預設不自動 commit）
- **[決策]** LLM 續用 OpenAI gpt-4o-mini 即可？還是要更好的（gpt-4o）或也想能切 Claude？
- **[內容]** 各隊主攻是否確認照建議（G1 雙機接力 / G2 幾何證明 / G3 鏡像教練 / G4 印尼語 / G5 法庭辯論）？確認後我寫初賽 3 頁教案草稿
- **[階2 環境]** ~~Unity 2022.3 是否已裝好、能出 Android APK?~~ ✅ **已確認(2026-06-19)**:KebbiBrainUnity 2022.3.62f3 已能出雙 APK。**剩**:你手邊有幾台可同網段(同 WiFi)測試的 Android 手機?(多機 UDP 互連測試需 ≥2 台真手機;模擬器多半不通)。
- **[安全]** 先前不慎外洩的 Azure / OpenAI 金鑰記得 rotate
