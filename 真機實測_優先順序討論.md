# 真機實測 — 優先順序討論稿

> 用途:拿到 claude.ai 跟它討論「真凱比上機時,該先測什麼」。
> 背景:2026-06-21 backlog 探索 loop 完成 18/18 項、自測 627/627 綠,**但全程未實機佈署**(遵「先不要實測」)。
> 所有邏輯核心都已在主控台用 Sim/假後端驗過;真正剩下的不確定性,集中在「真凱比硬體 + SDK 授權牆 + 雲端額度」三類。
> 這份不是定案,是**給你和 claude.ai 討論用的選項 + 我的建議 + 待你拍板的問題**。

---

## 一、為什麼需要實機(三類不確定性)

| 類別 | 不確定的事 | 主控台為何測不到 |
|---|---|---|
| **A. 硬體真值** | NeckZ 真實 ±40、底盤 turn() 方向/速度、馬達動作幅度與時序、DOA 解析度 | Sim 用文字模擬,角度/時間都是假的 |
| **B. SDK 授權牆** | 搶麥、底盤 turn、LED、原生臉、觸控回呼,是否被 active-code 授權牆 **silent fail**(不報錯、就是沒反應) | 只有真 NuwaSDK aar 在真機上跑才知道 |
| **C. 雲端額度/延遲** | Robotics-ER 免費額度(已知 4 秒就 429)、視覺延遲 2.4–4.8s、Live session 時長上限/掉線 | 額度/延遲是 Google 端即時狀態 |

**關鍵洞察**:B 類(授權牆 silent fail)是**最該先驗的**——因為它會「無聲地廢掉一整類功能」。一個 5 分鐘的探針測試,就能決定後面幾天的方向。

---

## 二、實機測試完整盤點

| 測項 | 影響的功能 | 需要什麼 | 風險/未知 | 主控台現況 |
|---|---|---|---|---|
| **① 搶麥**(系統 wakeup 持麥 → `Nuwa.stopListen()` 再 `Microphone.Start`) | Counselor、ConverseStt、StoryCircle、所有 STT 對話 | 1 台真凱比 | **高**:silent fail 則所有語音輸入全廢 | VAD 動態窗(#6)已備,搶麥前置未接 |
| **② 馬達轉向**(NeckZ ±40 夾限、底盤 `turn()` sign/速、`FaceFully` 合成面向) | EmpathyBody①、RoboGuide②、StoryCircle點名、SafetyEye、TebakArah | 1 台真凱比(輪式可底盤補、H201 桌上型只有頭) | **中**:角度/sign 要現場校;授權牆可能擋 turn | `RoboGuideMath`/`KebbiHead` 純函式已驗,真值未校 |
| **③ 觸控 `onTouchEvent`**(摸頭/握手/摸肚) | 觸控輸入層 #10、交棒 token | 1 台真凱比 + 先跑參考專案 `touching.sh` | **中**:授權牆 silent fail 風險 | `ITouchSensor`/狀態機 Sim 已驗,真事件可達性未知 |
| **④ 具身共情幅度/時序**(NeckY 點頭/前傾、ShoulderY) | EmpathyBody #1 | 1 台真凱比 | **低**:退化純語音仍可用;只是幅度要順 | `EmpathyGestures` 燈號→序列已驗,|deg|≤25 保守 |
| **⑤ 跨機 UDP**(遺失/亂序、PeerRegistry 收送) | TranslateRelay #11 雙機、Converse、多機遊戲 | **2 台**真機 + 同 WiFi | **中**:之前必測④只驗過 ping;此 WiFi 曾丟廣播(已加 unicast 備援) | `RobotLinkProtocol` 編解碼 + 真 loopback(#12)已驗,真跨機未測 |
| **⑥ Robotics-ER 視覺**(相機光軸對 NeckZ 零點校、429 額度、延遲、前/後鏡頭鏡像) | RoboGuide②、RoboVisionTalk#14、SafetyEye#18 | 1 台真凱比 + **Google Cloud 開帳單**(連續用) | **中**:免費額度緊;定點低頻可,即時追蹤不行 | 座標→角度/presence 解析純函式已驗 |
| **⑦ Gemini Live 無縫重連**(背景化/掉線→帶 handle 重連、ring buffer/重採樣相位重置) | LiveConversation、RoboVisionTalk | 1 台 Android + 較長時間 | **中高**:相位重置最易出 bug;OnApplicationPause 已備、完整重連迴圈未做 | `LiveResumeState` 協定/狀態已驗 |
| **⑧ Counselor 端到端**(真 STT 時機 + 紅線升級 + 落檔 + 具身共情整合) | 輔導室陪伴(旗艦) | 1 台真凱比(或 Android 中介) | **中**:`SafetyGateCore` 已 console 證明;整合時機未驗 | 紅線邏輯 627 全綠;之前 ASUS 背景化沒乾淨抓到紅線 |
| **⑨ LED / 原生臉**(`setLedColor`/`playFaceAnimation`) | EmpathyBody 加分、情緒表達 | 1 台真凱比 | **高(被擋機率)**:async 命令、無授權幾乎確定 silent fail | 刻意未做;列加分項 |

---

## 三、我的建議優先序(待討論)

### 🔴 P0 — 先驗「前提」(便宜、會無聲廢掉整類功能的不確定性)
**目標:用最少時間,確認 SDK 授權牆放不放行。一旦某項 silent fail,後面的整合都要改路線。**
1. **① 搶麥** — 一個 30 秒測試:`stopListen()` → 錄 → STT 出文字?→ 決定所有語音功能可不可行。
2. **② 馬達轉向 + ③ 觸控** — 命令 NeckZ→±40、底盤 turn、摸頭觸發 → 觀察有沒有動/有沒有事件。決定所有具身 + 觸控功能。

> P0 三項合計約 15–20 分鐘,**ROI 最高**:它把「未知」變「已知」,避免在會 silent fail 的路上投入幾天。

### 🟡 P1 — 旗艦功能端到端(P0 通過後)
4. **⑧ Counselor 端到端 + ④ 具身共情幅度** — 專案最高價值場景;需要 P0①(搶麥)+②(轉向)。實際跑一段 🟢→🟡→🔴,順具身動作幅度。
5. **⑥ RoboGuide 指物**(相機光軸校 + 定點認物) — 需要 P0② + 開帳單;最有「只有機器人做得到」說服力的視覺 demo。

### 🟢 P2 — 需額外資源 / 兩台 / 長時
6. **⑤ 跨機 UDP + TranslateRelay 雙機**(中文老師↔印尼學生) — 需 2 台。
7. **⑥ RoboVisionTalk / SafetyEye**(看著物件對話 / 視覺安全之眼) — 需開帳單 + 相機;隱私之眼還要現場告知同意。
8. **⑦ Live 無縫重連** — 較長時間穩定性測試,排最後。
9. **⑨ LED / 原生臉** — 高機率被擋,當「打通授權牆後的加分」,不擋路。

### ⚙️ 決策先行(非實測,但 gating 上線)
- **Google Cloud 開帳單**:解 ER 額度 + ephemeral token(#16)。**gating ⑥⑦**。
- **安全關鍵字內容定稿**(輔導專業):#8 機制已備,內容 gating Counselor 上線。
- **會談記錄隱私規範**(存哪/誰讀/留多久,#5)+ **視覺用於輔導的現場告知同意**(#18):gating ⑧⑦。

---

## 四、建議的「第一次實機 session」腳本(約 30 分鐘,只做 P0)

> 一台真凱比、接 adb,照這個跑,就能決定後面方向:

1. **搶麥①**(~10 min):裝 Counselor APK → 無聲模式打字確認 LLM 通 → 切有聲,講一句 → 看 logcat STT 有沒有出文字。出 = 搶麥 OK;不出 = 試 `stopListen()` 前置或排除權限框暫停。
2. **馬達②**(~10 min):跑 G3 或 RoboGuide → 命令 NeckZ 轉 +40/−40、底盤 turn → 目視角度對不對、sign 對不對 → 記下校準值。
3. **觸控③**(~5 min):先跑參考專案 `~/Projects/UnityKebbi` 的 `touching.sh` 確認事件可達 → 再裝觸控 APK 摸頭看 logcat 有沒有 `onTouchEvent`。
4. **結論**(~5 min):三項各記 ✅/❌/需 workaround → 更新「必測①③⑤」狀態 → 決定 P1 路線。

---

## 五、想跟你/claude.ai 確認的問題

1. **手上有幾台真凱比?** 輪式(可底盤補轉向)還是 H201 桌上型(只有頭、NeckZ ±40 夾限)?→ 決定 ②⑤ 怎麼測。
2. **有沒有 2 台**(同一型或混搭)?→ 決定 ⑤ 跨機 UDP / 口譯橋能不能測。
3. **要不要開 Google Cloud 帳單?** 不開 = ⑥⑦ 只能定點低頻試額度;開 = 可連續課堂展示 + ephemeral token。
4. **測試場合**:工作坊/實驗室一次測完,還是分批?→ 影響 P0/P1/P2 怎麼切。
5. **旗艦優先**:你想先把哪個做成「可給人看的 demo」——輔導室陪伴(⑧)、雙語口譯橋(⑤)、還是視覺認物指認(⑥)?→ 我據此把該鏈路的實機項往前提。

---

### 附:目前「不需實機就已綠」的對照(讓討論聚焦在真正要上機的部分)
所有純邏輯都已 627/627 主控台驗過:安全閘三層分級、退避守門(429/5xx)、VAD 端點、座標→角度、場景節流、presence 去抖、輪替/交棒/翻譯狀態機、UDP 編解碼、金鑰偵測、preflight、Live 重連協定…… → **這些不必再上機驗邏輯**,實機只需驗上表 ①–⑨ 的「硬體/授權牆/額度」真值。
